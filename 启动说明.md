# 启动说明

本文档记录当前项目在 Windows 本机开发环境下的启动方式，并配套根目录脚本 `启动项目.ps1`。

项目根目录：

```powershell
E:\nsh\nshls\RuoYi-Vue3-FastAPI-master
```

## 一键启动

在项目根目录执行：

```powershell
powershell -ExecutionPolicy Bypass -File .\启动项目.ps1
```

如果启动完成后希望自动打开浏览器：

```powershell
powershell -ExecutionPolicy Bypass -File .\启动项目.ps1 -Open
```

脚本会自动完成这些事：

- 检查后端虚拟环境、前端依赖、MySQL、Redis。
- 如果 `9099` 已经有后端监听，则不重复启动后端。
- 如果 `80` 已经有前端监听，则不重复启动前端。
- 后台启动后端和前端，并把日志写入 `logs\startup\`。
- 验证首页、数据分析页、后端文档是否可访问。

## 服务地址

- 前端首页：`http://127.0.0.1/`
- 数据分析页：`http://127.0.0.1/guild/analysis`
- 后端文档：`http://127.0.0.1:9099/docs`
- 后端端口：`9099`
- 前端 Vite 端口：`80`
- MySQL：`3306`
- Redis：`6379`

前端开发环境通过 `/dev-api` 代理到后端：

```text
/dev-api -> http://localhost:9099
```

## 环境要求

本机开发启动需要：

- Node.js 和 `npm`
- 前端依赖目录：`ruoyi-fastapi-frontend\node_modules`
- Python 虚拟环境：`ruoyi-fastapi-backend\.venv`
- MySQL 正在监听 `3306`
- Redis 正在监听 `6379`

后端开发配置文件：

```text
ruoyi-fastapi-backend\.env.dev
```

常用配置：

```text
APP_PORT = 9099
DB_HOST = 127.0.0.1
DB_PORT = 3306
DB_USERNAME = root
DB_PASSWORD = 123456
DB_DATABASE = ruoyi
REDIS_HOST = 127.0.0.1
REDIS_PORT = 6379
REDIS_DATABASE = 2
```

前端开发配置文件：

```text
ruoyi-fastapi-frontend\.env.development
```

常用配置：

```text
VITE_APP_BASE_API = /dev-api
```

## 手动启动后端

```powershell
cd E:\nsh\nshls\RuoYi-Vue3-FastAPI-master\ruoyi-fastapi-backend
.\.venv\Scripts\python.exe app.py
```

后台启动并写日志：

```powershell
$env:PYTHONUTF8='1'
$env:PYTHONIOENCODING='utf-8'
Start-Process -FilePath "E:\nsh\nshls\RuoYi-Vue3-FastAPI-master\ruoyi-fastapi-backend\.venv\Scripts\python.exe" `
  -ArgumentList "app.py" `
  -WorkingDirectory "E:\nsh\nshls\RuoYi-Vue3-FastAPI-master\ruoyi-fastapi-backend" `
  -RedirectStandardOutput "E:\nsh\nshls\RuoYi-Vue3-FastAPI-master\logs\startup\backend.out.log" `
  -RedirectStandardError "E:\nsh\nshls\RuoYi-Vue3-FastAPI-master\logs\startup\backend.err.log" `
  -WindowStyle Hidden
```

## 手动启动前端

```powershell
cd E:\nsh\nshls\RuoYi-Vue3-FastAPI-master\ruoyi-fastapi-frontend
npm run dev -- --host 0.0.0.0 --port 80
```

后台启动并写日志：

```powershell
Start-Process -FilePath "npm.cmd" `
  -ArgumentList "run dev -- --host 0.0.0.0 --port 80" `
  -WorkingDirectory "E:\nsh\nshls\RuoYi-Vue3-FastAPI-master\ruoyi-fastapi-frontend" `
  -RedirectStandardOutput "E:\nsh\nshls\RuoYi-Vue3-FastAPI-master\logs\startup\frontend.out.log" `
  -RedirectStandardError "E:\nsh\nshls\RuoYi-Vue3-FastAPI-master\logs\startup\frontend.err.log" `
  -WindowStyle Hidden
```

## 验证

查看端口：

```powershell
netstat -ano | Select-String -Pattern ':80\s|:9099\s|:3306\s|:6379\s'
```

验证前端：

```powershell
Invoke-WebRequest -UseBasicParsing http://127.0.0.1/ | Select-Object StatusCode
Invoke-WebRequest -UseBasicParsing http://127.0.0.1/guild/analysis | Select-Object StatusCode
```

验证后端：

```powershell
Invoke-WebRequest -UseBasicParsing http://127.0.0.1:9099/docs | Select-Object StatusCode
```

正常应返回：

```text
StatusCode
----------
       200
```

## 查看日志

一键启动脚本会把日志写到：

```text
logs\startup\
```

查看最近日志：

```powershell
Get-ChildItem .\logs\startup\*.log | Sort-Object LastWriteTime -Descending | Select-Object -First 8 Name,LastWriteTime,Length
```

查看后端错误日志：

```powershell
Get-ChildItem .\logs\startup\backend-*.err.log |
  Sort-Object LastWriteTime -Descending |
  Select-Object -First 1 |
  ForEach-Object { Get-Content -LiteralPath $_.FullName -Tail 120 }
```

查看前端错误日志：

```powershell
Get-ChildItem .\logs\startup\frontend-*.err.log |
  Sort-Object LastWriteTime -Descending |
  Select-Object -First 1 |
  ForEach-Object { Get-Content -LiteralPath $_.FullName -Tail 120 }
```

## 停止服务

先查看端口对应 PID：

```powershell
netstat -ano | Select-String -Pattern ':80\s|:9099\s'
```

再停止指定 PID：

```powershell
Stop-Process -Id <PID> -Force
```

如果要确认进程路径：

```powershell
Get-CimInstance Win32_Process |
  Where-Object { $_.ProcessId -in @(<PID1>, <PID2>) } |
  Select-Object ProcessId,Name,CommandLine
```

## Docker 启动方案

项目根目录已有两个 compose 文件：

- MySQL 版本：`docker-compose.my.yml`
- PostgreSQL 版本：`docker-compose.pg.yml`

MySQL 版本：

```powershell
docker compose -f docker-compose.my.yml up -d --build
```

PostgreSQL 版本：

```powershell
docker compose -f docker-compose.pg.yml up -d --build
```

Docker 端口：

- 前端：`http://127.0.0.1:12580/`
- 后端：`http://127.0.0.1:19099/docs`
- Docker MySQL：`13306`
- Docker PostgreSQL：`15432`
- Docker Redis：`16379`

停止 Docker 服务：

```powershell
docker compose -f docker-compose.my.yml down
docker compose -f docker-compose.pg.yml down
```

## 常见问题

### 端口 80 被占用

```powershell
netstat -ano | Select-String -Pattern ':80\s'
```

如果确认是旧前端进程，可以停止对应 PID 后重新执行：

```powershell
powershell -ExecutionPolicy Bypass -File .\启动项目.ps1
```

### 后端 9099 没起来

先确认 MySQL 和 Redis：

```powershell
netstat -ano | Select-String -Pattern ':3306\s|:6379\s'
```

再查看后端错误日志。

### 前端页面打开但接口失败

先确认后端文档可访问：

```powershell
Invoke-WebRequest -UseBasicParsing http://127.0.0.1:9099/docs | Select-Object StatusCode
```

如果后端没起来，先修后端；如果后端正常，再检查：

```text
ruoyi-fastapi-frontend\vite.config.js
```

确认 `/dev-api` 代理到 `http://localhost:9099`。
