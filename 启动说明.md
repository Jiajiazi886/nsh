# 启动说明

本文档记录当前项目在本机 Windows 环境下的前后端启动方式。

项目根目录：

```powershell
E:\nsh\nshls\RuoYi-Vue3-FastAPI-master
```

## 1. 服务端口

- 前端开发服务：`http://127.0.0.1/`
- 前端分析页：`http://127.0.0.1/guild/analysis`
- 后端接口文档：`http://127.0.0.1:9099/docs`
- 后端服务端口：`9099`
- 前端 Vite 端口：`80`
- MySQL：`3306`
- Redis：`6379`

前端会把 `/dev-api` 代理到后端：

```text
/dev-api -> http://localhost:9099
```

## 2. 环境依赖

启动前确认本机已有：

- Node.js，可运行 `npm`
- Python 虚拟环境：`ruoyi-fastapi-backend\.venv`
- MySQL 正在监听 `3306`
- Redis 正在监听 `6379`

当前后端开发配置在：

```text
ruoyi-fastapi-backend\.env.dev
```

关键配置：

```text
APP_PORT = 9099
DB_HOST = 127.0.0.1
DB_PORT = 3306
DB_USERNAME = root
DB_PASSWORD = 123456
DB_DATABASE = ruoyi
REDIS_HOST = 127.0.0.1
REDIS_PORT = 6379
REDIS_DATABASE = 2
```

前端开发配置在：

```text
ruoyi-fastapi-frontend\.env.development
```

关键配置：

```text
VITE_APP_BASE_API = /dev-api
```

## 3. 启动后端

在项目根目录执行：

```powershell
cd E:\nsh\nshls\RuoYi-Vue3-FastAPI-master\ruoyi-fastapi-backend
.\.venv\Scripts\python.exe app.py
```

后端启动成功后，应能访问：

```text
http://127.0.0.1:9099/docs
```

如果希望后台启动并写日志：

```powershell
Start-Process -FilePath "E:\nsh\nshls\RuoYi-Vue3-FastAPI-master\ruoyi-fastapi-backend\.venv\Scripts\python.exe" `
  -ArgumentList "app.py" `
  -WorkingDirectory "E:\nsh\nshls\RuoYi-Vue3-FastAPI-master\ruoyi-fastapi-backend" `
  -RedirectStandardOutput "E:\nsh\nshls\RuoYi-Vue3-FastAPI-master\ruoyi-fastapi-backend\backend-dev.out.log" `
  -RedirectStandardError "E:\nsh\nshls\RuoYi-Vue3-FastAPI-master\ruoyi-fastapi-backend\backend-dev.err.log" `
  -WindowStyle Hidden
```

## 4. 启动前端

在项目根目录执行：

```powershell
cd E:\nsh\nshls\RuoYi-Vue3-FastAPI-master\ruoyi-fastapi-frontend
npm run dev -- --host 0.0.0.0 --port 80 --open false
```

前端启动成功后，应能访问：

```text
http://127.0.0.1/
http://127.0.0.1/guild/analysis
```

如果希望后台启动并写日志：

```powershell
Start-Process -FilePath "npm.cmd" `
  -ArgumentList "run dev -- --host 0.0.0.0 --port 80 --open false" `
  -WorkingDirectory "E:\nsh\nshls\RuoYi-Vue3-FastAPI-master\ruoyi-fastapi-frontend" `
  -RedirectStandardOutput "E:\nsh\nshls\RuoYi-Vue3-FastAPI-master\ruoyi-fastapi-frontend\frontend-dev.out.log" `
  -RedirectStandardError "E:\nsh\nshls\RuoYi-Vue3-FastAPI-master\ruoyi-fastapi-frontend\frontend-dev.err.log" `
  -WindowStyle Hidden
```

## 5. 验证服务是否启动

查看端口：

```powershell
netstat -ano | Select-String -Pattern ':80\s|:9099\s|:3306\s|:6379\s'
```

验证前端：

```powershell
Invoke-WebRequest -UseBasicParsing http://127.0.0.1/ | Select-Object StatusCode
Invoke-WebRequest -UseBasicParsing http://127.0.0.1/guild/analysis | Select-Object StatusCode
```

验证后端：

```powershell
Invoke-WebRequest -UseBasicParsing http://127.0.0.1:9099/docs | Select-Object StatusCode
```

正常情况下都应返回：

```text
StatusCode
----------
       200
```

## 6. 查看日志

后端日志：

```powershell
Get-Content -LiteralPath E:\nsh\nshls\RuoYi-Vue3-FastAPI-master\ruoyi-fastapi-backend\backend-dev.out.log -Tail 120
Get-Content -LiteralPath E:\nsh\nshls\RuoYi-Vue3-FastAPI-master\ruoyi-fastapi-backend\backend-dev.err.log -Tail 120
```

前端日志：

```powershell
Get-Content -LiteralPath E:\nsh\nshls\RuoYi-Vue3-FastAPI-master\ruoyi-fastapi-frontend\frontend-dev.out.log -Tail 80
Get-Content -LiteralPath E:\nsh\nshls\RuoYi-Vue3-FastAPI-master\ruoyi-fastapi-frontend\frontend-dev.err.log -Tail 80
```

## 7. 停止服务

先查看进程：

```powershell
Get-Process python,node -ErrorAction SilentlyContinue | Select-Object Id,ProcessName,Path
```

停止指定 PID：

```powershell
Stop-Process -Id <PID> -Force
```

如果只想停止当前项目相关进程，建议先确认 `Path` 是否指向本项目目录，避免误杀其他 Node 或 Python 程序。

## 8. 常见问题

### 端口 80 被占用

查看占用：

```powershell
netstat -ano | Select-String -Pattern ':80\s'
```

如果已有旧前端进程在运行，可以停止对应 PID 后重新启动。

### 后端 9099 没起来

先确认 MySQL 和 Redis 是否监听：

```powershell
netstat -ano | Select-String -Pattern ':3306\s|:6379\s'
```

再看后端错误日志：

```powershell
Get-Content -LiteralPath E:\nsh\nshls\RuoYi-Vue3-FastAPI-master\ruoyi-fastapi-backend\backend-dev.err.log -Tail 120
```

### 前端页面能打开但接口失败

检查后端是否可访问：

```powershell
Invoke-WebRequest -UseBasicParsing http://127.0.0.1:9099/docs | Select-Object StatusCode
```

检查前端代理配置：

```text
ruoyi-fastapi-frontend\vite.config.js
```

其中 `/dev-api` 应代理到：

```text
http://localhost:9099
```

## 9. 本次已验证状态

本机已验证：

- `http://127.0.0.1/` 返回 `200`
- `http://127.0.0.1/guild/analysis` 返回 `200`
- `http://127.0.0.1:9099/docs` 返回 `200`

启动时观察到的进程示例：

```text
前端 Vite：node，监听 80
后端服务：python，监听 9099
```
