# 用户量增长与帮会数据扩容风险分析

生成时间：2026-06-16 21:31:04

分析对象：`ruoyi` 数据库当前 34 张表，以及帮会成员、约战报名、约战排表、历史排表、数据导入、Univer 自由表格这些核心链路。

## 一、当前规模快照

| 指标 | 当前值 | 说明 |
|---|---:|---|
| `sys_user` 行数 | 20 | 系统用户表。 |
| `guild_member` 行数 | 69 | 帮会成员主数据表，保存玩家名称、职业、副职、状态等，是排表左侧对象抽屉的数据来源。 |
| `guild_battle` 行数 | 5 | 帮会约战/数据导入主表，承载一次约战或一次导入数据的元信息。 |
| `guild_battle_record` 行数 | 600 | 约战玩家明细数据表，是数据分析的核心事实表，保存每名玩家的击败、助攻、伤害、治疗、承伤等统计。 |
| `guild_battle_registration` 行数 | 24 | 约战报名表，记录玩家通过邀请链接提交的报名、请假、审核状态。 |
| `guild_schedule` 行数 | 9 | 约战排表主表，区分当前排表和历史排表，是团队、小队、自由表格 workbook 的上层记录。 |
| `guild_schedule_assignment` 行数 | 3 | 约战排表成员分配表，保存成员被排到哪个团队/小队/槽位。 |
| `guild_schedule_workbook` 行数 | 1 | 约战排表自由 Univer 表格数据表，按 schedule_id 保存整张 workbook_json，用于类 Excel 自由编辑和历史还原。 |
| `sys_menu` 行数 | 115 | 系统菜单和权限标识表。 |

当前数据量还很小，但业务模型已经具备明显的放大效应：一个用户可以有多个帮会/约战；一个帮会可以有大量成员；一次约战会产生大量玩家明细；一个排表会产生结构化槽位和整张 Univer workbook；历史排表会把这些数据复制或快照化。

## 二、数据增长会先打到哪些地方

| 增长来源 | 直接膨胀的表 | 典型问题 |
|---|---|---|
| 用户变多 | `sys_user`、`sys_user_role`、`sys_logininfor`、`sys_oper_log` | 登录/操作日志增长快，权限查询和后台列表需要分页与索引。 |
| 帮会成员变多 | `guild_member`、`guild_join_application`、`guild_battle_registration` | 左侧成员抽屉一次性加载会卡；按职业分组、搜索、审核筛选会变慢。 |
| 约战数据导入变多 | `guild_battle`、`guild_battle_record` | 数据分析聚合扫描变重；玩家名、帮会名、职业维度统计需要复合索引或预聚合。 |
| 排表变多 | `guild_schedule`、`guild_schedule_team`、`guild_schedule_squad`、`guild_schedule_assignment` | 当前排表和历史排表混在一起时，查询如果不按用户/当前状态/时间过滤会越来越慢。 |
| 自由表格变多 | `guild_schedule_workbook` | `workbook_json` 是大文本，频繁保存会造成数据库 I/O、binlog、备份体积和网络传输压力。 |
| 历史排表变多 | `guild_schedule_*`、`guild_schedule_workbook` | 历史列表、复制历史、删除历史、应用历史都可能拖慢，且容易产生孤儿数据。 |

## 三、主要风险

### 1. 多租户隔离风险

系统里存在用户、帮会、成员、约战、排表多层归属关系。用户量变大后，如果某个查询忘记带 `user_id`、`owner_user_id`、`guild_id` 或当前用户权限过滤，就会出现两个问题：一是跨用户看到别人的数据，二是数据库不得不扫全表。这个风险比单纯慢查询更严重。

建议：所有帮会业务查询统一从当前登录用户派生租户条件；服务层封装公共过滤器；给核心列表接口补充权限回归测试。

### 2. 成员抽屉和职业文件夹会变卡

当一个帮会从几十人变成几百、几千人，前端一次性加载全部成员、再在浏览器里分组、搜索、拖拽，会导致页面首次渲染慢、拖拽卡顿、内存升高。

建议：成员接口支持分页、按职业懒加载、服务端搜索；左侧对象抽屉使用虚拟列表；已排状态只返回必要字段，不要每次带全量排表 workbook。

### 3. 约战明细会成为分析瓶颈

`guild_battle_record` 是事实表。每次导入都按玩家产生明细，后续又要按玩家、职业、帮会、队伍、战斗、历史排表做聚合。数据量上来后，直接实时扫明细表会让分析页面加载越来越慢。

建议：给 `battle_id + player_name`、`battle_id + player_class`、`guild_id + battle_date`、`battle_id + guild_name` 这类高频条件建立复合索引；常用分析结果做汇总表或缓存；大分析改成异步任务，前端轮询结果。

### 4. Univer workbook 大文本会放大写入压力

当前 `guild_schedule_workbook.workbook_json` 用整张 workbook JSON 持久化。用户自由编辑越多、样式越多、历史越多，单行文本会越来越大。自动保存如果每次都 PUT 整个 JSON，会造成大对象频繁写入，拖慢 MySQL、增加 binlog、备份和恢复成本。

建议：只保存稀疏 `cellData` 和必要样式，不保存 2000x200 的空白结构；自动保存做 800-1500ms 防抖和内容 hash 去重；增加 `version` 做乐观锁；限制单个 workbook 最大体积；长期考虑压缩存储或对象存储。

### 5. 历史排表会造成快照膨胀

保存历史如果复制团队、小队、成员分配和整张 workbook，历史越多，数据越大。用户还会希望历史查询、重命名、删除、应用历史都很快。如果删除只删主表，不清理子表和 workbook，会留下垃圾数据。

建议：历史表按 `user_id + create_time` 分页查询；删除历史必须事务内清理 `team/squad/assignment/workbook`；历史详情懒加载；大 workbook 历史可以压缩或归档。

### 6. MyISAM 表会影响一致性和并发

当前库里 `guild_schedule_workbook`、`guild_class_color`、`guild_join_application`、`guild_profession` 等表是 MyISAM。MyISAM 不支持事务和行级锁。排表 workbook 属于频繁写入数据，用 MyISAM 时并发保存、删除历史、应用历史更容易出现一致性问题。

建议：业务写入表优先统一为 InnoDB，尤其是 `guild_schedule_workbook`、报名和申请类表。

### 7. 连接池和并发配置可能提前撞墙

当前 `.env.dev` 中数据库连接池是 `DB_POOL_SIZE=50`、`DB_MAX_OVERFLOW=10`。如果后端多进程部署，每个进程都会持有自己的连接池，很容易超过 MySQL `max_connections`。

建议：按部署进程数重新计算连接池；给慢接口加缓存和异步任务，避免每个请求长时间占用连接；监控连接数、等待连接耗时和慢 SQL。

### 8. 日志表需要生命周期

`sys_oper_log`、`sys_logininfor`、`sys_job_log` 当前为空或很小，但真实使用后增长会非常快。日志表如果不清理，会影响备份、迁移和后台查询。

建议：设置 30/90/180 天保留策略；按时间清理或归档；日志查询必须按时间范围分页。

## 四、优先级建议

| 优先级 | 建议 | 原因 |
|---|---|---|
| P0 | 所有帮会业务接口强制带当前用户/帮会过滤 | 防止越权和全表扫描。 |
| P0 | `guild_schedule_workbook` 改 InnoDB，并保证历史删除事务清理 | 排表是核心写入链路，不能依赖非事务表。 |
| P0 | 历史排表列表分页，workbook 详情懒加载 | 历史多以后最容易拖垮页面。 |
| P1 | `guild_battle_record` 增加分析维度复合索引 | 数据分析会随着导入数据线性变慢。 |
| P1 | 成员抽屉改分页/虚拟列表/职业懒加载 | 帮会成员多时前端体验会明显下降。 |
| P1 | workbook 保存改稀疏数据、防抖、hash 去重、版本号 | 避免每次编辑都写巨大 JSON。 |
| P2 | 定期归档日志和旧历史 | 控制备份、恢复和后台查询成本。 |
| P2 | 建立分析汇总表或缓存 | 用户量增长后保障分析页响应速度。 |

## 五、推荐索引方向

下面是方向，不建议不看 SQL 执行计划就一次性全加。上线前应使用 `EXPLAIN` 校验实际查询是否命中。

- `guild_member`：按当前用户/帮会、删除标记、职业、玩家名查询。
- `guild_battle`：按当前用户/帮会、删除标记、约战时间、创建时间查询。
- `guild_battle_record`：按 `battle_id`、玩家名、职业、帮会名、导入时间进行复合索引设计。
- `guild_battle_registration`：按邀请链接、审核状态、当前用户/帮会、删除标记查询。
- `guild_schedule`：按当前用户、当前排表标记、创建时间、历史状态查询。
- `guild_schedule_team/squad/assignment`：按 `schedule_id`、`team_id`、`squad_id`、`member_id` 查询。
- `guild_schedule_workbook`：保留 `schedule_id` 唯一索引，并按 `update_time` 支持维护任务。

## 六、建议的阶段性演进

### 近期

- 补齐历史删除、历史重命名、workbook 保存的一致性测试。
- 把 `guild_schedule_workbook` 改成 InnoDB。
- 给成员列表、历史列表、战斗数据列表增加分页和搜索。
- 打开 MySQL 慢查询日志，记录超过 500ms 的 SQL。

### 中期

- 数据分析从实时扫明细升级为缓存/汇总表。
- workbook 存储从整张 JSON 改为稀疏快照或差量保存。
- 前端成员抽屉和历史排表使用虚拟列表。
- 增加数据库体积、连接池、慢 SQL、接口 p95 延迟监控。

### 长期

- 大表按时间或租户归档。
- 分析任务异步化，复杂计算放到任务队列。
- 大 workbook 或历史附件迁移到对象存储，数据库只保留索引和元数据。
- 多租户权限规则固化成统一数据访问层，减少业务代码漏过滤。

