# 数据库表结构说明

生成时间：2026-06-16 21:31:04

数据来源：已实际连接本机 MySQL `127.0.0.1:3306`，数据库 `ruoyi`，用户 `root`。
MySQL 版本：`8.0.12`。当前检测到 `34` 张表。

> 说明：`实际行数` 来自 `select count(*)`，比 `information_schema.TABLE_ROWS` 更适合当前小库排查。字段注释来自数据库自身 comment，业务用途根据表名、现有代码和当前系统功能整理。

## 一、数据库整体用途

这个数据库同时承载两类数据：

- 帮会业务数据：成员、职业、报名、约战导入数据、数据分析、约战排表、历史排表、Univer 自由表格。
- 若依系统数据：用户、角色、菜单、字典、配置、日志、定时任务和代码生成配置。

## 二、表清单总览

| 领域 | 表名 | 引擎 | 实际行数 | 数据KB | 索引KB | 数据库注释 | 主要用途 |
|---|---:|---:|---:|---:|---:|---|---|
| 任务调度 | `apscheduler_jobs` | MyISAM | 0 | 0.0 | 4.0 |  | APScheduler 持久化任务表，保存 Python 定时任务的 job_state。 |
| 代码生成 | `gen_table` | InnoDB | 0 | 16.0 | 0.0 | 代码生成业务表 | 若依代码生成主表，记录可生成代码的业务表配置。 |
| 代码生成 | `gen_table_column` | InnoDB | 0 | 16.0 | 0.0 | 代码生成业务表字段 | 若依代码生成字段表，记录每个业务表字段的生成规则。 |
| 帮会约战与分析 | `guild_battle` | InnoDB | 5 | 16.0 | 48.0 | 帮会约战表 | 帮会约战/数据导入主表，承载一次约战或一次导入数据的元信息。 |
| 帮会约战与分析 | `guild_battle_invite` | InnoDB | 11 | 16.0 | 48.0 | 约战临时邀请链接表 | 约战报名邀请链接表，用于临时报名入口、邀请码和过期控制。 |
| 帮会约战与分析 | `guild_battle_record` | InnoDB | 600 | 128.0 | 64.0 | 约战玩家明细表 | 约战玩家明细数据表，是数据分析的核心事实表，保存每名玩家的击败、助攻、伤害、治疗、承伤等统计。 |
| 帮会报名 | `guild_battle_registration` | InnoDB | 24 | 16.0 | 80.0 | 约战报名表 | 约战报名表，记录玩家通过邀请链接提交的报名、请假、审核状态。 |
| 帮会基础资料 | `guild_class_color` | MyISAM | 35 | 1.9 | 3.0 | 职业颜色配置表 | 职业颜色配置表，给职业卡片、表格单元格和分析视图提供视觉颜色。 |
| 帮会基础资料 | `guild_info` | InnoDB | 0 | 16.0 | 16.0 | 帮会信息表 | 帮会信息表，保存帮会基础资料。当前库中暂无数据。 |
| 帮会报名 | `guild_join_application` | MyISAM | 7 | 0.5 | 2.0 | 帮会入会申请表 | 帮会入会申请表，记录玩家申请加入帮会的资料与审核过程。 |
| 帮会基础资料 | `guild_member` | InnoDB | 69 | 16.0 | 32.0 | 帮会成员表 | 帮会成员主数据表，保存玩家名称、职业、副职、状态等，是排表左侧对象抽屉的数据来源。 |
| 帮会基础资料 | `guild_profession` | MyISAM | 12 | 0.8 | 3.0 | 职业信息表 | 职业信息表，维护职业名称、排序、职业颜色等基础配置。 |
| 帮会审核 | `guild_review` | InnoDB | 0 | 16.0 | 48.0 | 审核记录表 | 审核记录表，用于记录帮会相关审核动作。当前库中暂无数据。 |
| 约战排表 | `guild_schedule` | InnoDB | 9 | 16.0 | 16.0 | 约战排表主表 | 约战排表主表，区分当前排表和历史排表，是团队、小队、自由表格 workbook 的上层记录。 |
| 约战排表 | `guild_schedule_assignment` | InnoDB | 3 | 16.0 | 48.0 | 约战排表成员分配表 | 约战排表成员分配表，保存成员被排到哪个团队/小队/槽位。 |
| 约战排表 | `guild_schedule_squad` | InnoDB | 4 | 16.0 | 16.0 | 约战排表小队表 | 约战排表小队表，属于某个团队，通常承载 1-6 号位。 |
| 约战排表 | `guild_schedule_team` | InnoDB | 5 | 16.0 | 16.0 | 约战排表团队表 | 约战排表团队表，属于某个排表，下面包含多个小队。 |
| 约战排表 | `guild_schedule_workbook` | MyISAM | 1 | 10.8 | 3.0 | 约战排表自由表格数据表 | 约战排表自由 Univer 表格数据表，按 schedule_id 保存整张 workbook_json，用于类 Excel 自由编辑和历史还原。 |
| 帮会基础资料 | `guild_team` | InnoDB | 0 | 16.0 | 16.0 | 帮会团队表 | 帮会固定团队表，当前库中暂无数据。 |
| 系统权限与运维 | `sys_config` | InnoDB | 9 | 16.0 | 0.0 | 参数配置表 | 系统参数配置表。 |
| 系统权限与运维 | `sys_dept` | MyISAM | 0 | 0.0 | 1.0 | 部门表 | 系统部门表。 |
| 系统权限与运维 | `sys_dict_data` | InnoDB | 69 | 16.0 | 0.0 | 字典数据表 | 系统字典数据表，保存具体字典项。 |
| 系统权限与运维 | `sys_dict_type` | InnoDB | 12 | 16.0 | 16.0 | 字典类型表 | 系统字典类型表，定义字典分类。 |
| 系统权限与运维 | `sys_job` | InnoDB | 4 | 16.0 | 0.0 | 定时任务调度表 | 系统定时任务配置表。 |
| 系统权限与运维 | `sys_job_log` | InnoDB | 0 | 16.0 | 0.0 | 定时任务调度日志表 | 系统定时任务日志表。 |
| 系统权限与运维 | `sys_logininfor` | InnoDB | 0 | 16.0 | 32.0 | 系统访问记录 | 系统登录日志表。 |
| 系统权限与运维 | `sys_menu` | InnoDB | 115 | 16.0 | 0.0 | 菜单权限表 | 系统菜单和权限标识表。 |
| 系统权限与运维 | `sys_notice` | InnoDB | 1 | 16.0 | 0.0 | 通知公告表 | 系统通知公告表。 |
| 系统权限与运维 | `sys_oper_log` | InnoDB | 0 | 16.0 | 48.0 | 操作日志记录 | 系统操作日志表。 |
| 系统权限与运维 | `sys_role` | InnoDB | 3 | 16.0 | 0.0 | 角色信息表 | 系统角色表。 |
| 系统权限与运维 | `sys_role_dept` | MyISAM | 0 | 0.0 | 1.0 | 角色和部门关联表 | 角色和部门关联表。 |
| 系统权限与运维 | `sys_role_menu` | InnoDB | 84 | 16.0 | 0.0 | 角色和菜单关联表 | 角色和菜单关联表。 |
| 系统权限与运维 | `sys_user` | InnoDB | 20 | 16.0 | 0.0 | 用户信息表 | 系统用户表。 |
| 系统权限与运维 | `sys_user_role` | InnoDB | 3 | 16.0 | 0.0 | 用户和角色关联表 | 用户和角色关联表。 |

## 三、按业务领域说明

### 帮会基础资料

- `guild_class_color`：职业颜色配置表，给职业卡片、表格单元格和分析视图提供视觉颜色。 当前 `35` 行。
- `guild_info`：帮会信息表，保存帮会基础资料。当前库中暂无数据。 当前 `0` 行。
- `guild_member`：帮会成员主数据表，保存玩家名称、职业、副职、状态等，是排表左侧对象抽屉的数据来源。 当前 `69` 行。
- `guild_profession`：职业信息表，维护职业名称、排序、职业颜色等基础配置。 当前 `12` 行。
- `guild_team`：帮会固定团队表，当前库中暂无数据。 当前 `0` 行。

### 帮会报名

- `guild_battle_registration`：约战报名表，记录玩家通过邀请链接提交的报名、请假、审核状态。 当前 `24` 行。
- `guild_join_application`：帮会入会申请表，记录玩家申请加入帮会的资料与审核过程。 当前 `7` 行。

### 帮会约战与分析

- `guild_battle`：帮会约战/数据导入主表，承载一次约战或一次导入数据的元信息。 当前 `5` 行。
- `guild_battle_invite`：约战报名邀请链接表，用于临时报名入口、邀请码和过期控制。 当前 `11` 行。
- `guild_battle_record`：约战玩家明细数据表，是数据分析的核心事实表，保存每名玩家的击败、助攻、伤害、治疗、承伤等统计。 当前 `600` 行。

### 约战排表

- `guild_schedule`：约战排表主表，区分当前排表和历史排表，是团队、小队、自由表格 workbook 的上层记录。 当前 `9` 行。
- `guild_schedule_assignment`：约战排表成员分配表，保存成员被排到哪个团队/小队/槽位。 当前 `3` 行。
- `guild_schedule_squad`：约战排表小队表，属于某个团队，通常承载 1-6 号位。 当前 `4` 行。
- `guild_schedule_team`：约战排表团队表，属于某个排表，下面包含多个小队。 当前 `5` 行。
- `guild_schedule_workbook`：约战排表自由 Univer 表格数据表，按 schedule_id 保存整张 workbook_json，用于类 Excel 自由编辑和历史还原。 当前 `1` 行。

### 帮会审核

- `guild_review`：审核记录表，用于记录帮会相关审核动作。当前库中暂无数据。 当前 `0` 行。

### 系统权限与运维

- `sys_config`：系统参数配置表。 当前 `9` 行。
- `sys_dept`：系统部门表。 当前 `0` 行。
- `sys_dict_data`：系统字典数据表，保存具体字典项。 当前 `69` 行。
- `sys_dict_type`：系统字典类型表，定义字典分类。 当前 `12` 行。
- `sys_job`：系统定时任务配置表。 当前 `4` 行。
- `sys_job_log`：系统定时任务日志表。 当前 `0` 行。
- `sys_logininfor`：系统登录日志表。 当前 `0` 行。
- `sys_menu`：系统菜单和权限标识表。 当前 `115` 行。
- `sys_notice`：系统通知公告表。 当前 `1` 行。
- `sys_oper_log`：系统操作日志表。 当前 `0` 行。
- `sys_role`：系统角色表。 当前 `3` 行。
- `sys_role_dept`：角色和部门关联表。 当前 `0` 行。
- `sys_role_menu`：角色和菜单关联表。 当前 `84` 行。
- `sys_user`：系统用户表。 当前 `20` 行。
- `sys_user_role`：用户和角色关联表。 当前 `3` 行。

### 任务调度

- `apscheduler_jobs`：APScheduler 持久化任务表，保存 Python 定时任务的 job_state。 当前 `0` 行。

### 代码生成

- `gen_table`：若依代码生成主表，记录可生成代码的业务表配置。 当前 `0` 行。
- `gen_table_column`：若依代码生成字段表，记录每个业务表字段的生成规则。 当前 `0` 行。

## 四、关键业务关系

- `guild_member` 是帮会玩家主数据，报名、排表和分析都需要用玩家名称、职业、副职等字段进行展示或匹配。
- `guild_battle` 是一次约战/导入数据的主记录，`guild_battle_record` 是玩家级统计明细，数据分析主要围绕这两张表聚合。
- `guild_battle_invite` 和 `guild_battle_registration` 负责约战报名链路，报名审核通过后可以进入排表。
- `guild_schedule` 是排表主记录，`guild_schedule_team`、`guild_schedule_squad`、`guild_schedule_assignment` 是结构化团队/小队/成员槽位。
- `guild_schedule_workbook` 是自由表格层，保存 Univer workbook JSON，用于自由排表、备注、颜色、历史还原。它和结构化排表并存。
- `sys_user`、`sys_role`、`sys_menu`、`sys_user_role`、`sys_role_menu` 支撑后台账号、权限和菜单展示。

数据库中声明的外键：

当前数据库未检测到显式外键约束。业务关系主要由代码维护，因此删除历史、删除成员、清理排表时要特别注意孤儿数据。

## 五、每张表字段与索引

### `apscheduler_jobs`

用途：APScheduler 持久化任务表，保存 Python 定时任务的 job_state。

引擎：`MyISAM`；实际行数：`0`；数据库注释：无。

字段：

| 字段 | 类型 | 空 | 键 | 默认值 | 额外 | 注释 |
|---|---|---|---|---|---|---|
| `id` | `varchar(191)` | NO | PRI | NULL |  |  |
| `next_run_time` | `double` | YES | MUL | NULL |  |  |
| `job_state` | `blob` | NO |  | NULL |  |  |

索引：

| 索引名 | 唯一 | 字段 |
|---|---:|---|
| `PRIMARY` | 是 | `id` |
| `ix_apscheduler_jobs_next_run_time` | 否 | `next_run_time` |

### `gen_table`

用途：若依代码生成主表，记录可生成代码的业务表配置。

引擎：`InnoDB`；实际行数：`0`；数据库注释：代码生成业务表。

字段：

| 字段 | 类型 | 空 | 键 | 默认值 | 额外 | 注释 |
|---|---|---|---|---|---|---|
| `table_id` | `bigint(20)` | NO | PRI | NULL | auto_increment | 编号 |
| `table_name` | `varchar(200)` | YES |  |  |  | 表名称 |
| `table_comment` | `varchar(500)` | YES |  |  |  | 表描述 |
| `sub_table_name` | `varchar(64)` | YES |  | NULL |  | 关联子表的表名 |
| `sub_table_fk_name` | `varchar(64)` | YES |  | NULL |  | 子表关联的外键名 |
| `class_name` | `varchar(100)` | YES |  |  |  | 实体类名称 |
| `tpl_category` | `varchar(200)` | YES |  | crud |  | 使用的模板（crud单表操作 tree树表操作） |
| `tpl_web_type` | `varchar(30)` | YES |  |  |  | 前端模板类型（element-ui模版 element-plus模版） |
| `package_name` | `varchar(100)` | YES |  | NULL |  | 生成包路径 |
| `module_name` | `varchar(30)` | YES |  | NULL |  | 生成模块名 |
| `business_name` | `varchar(30)` | YES |  | NULL |  | 生成业务名 |
| `function_name` | `varchar(50)` | YES |  | NULL |  | 生成功能名 |
| `function_author` | `varchar(50)` | YES |  | NULL |  | 生成功能作者 |
| `gen_type` | `char(1)` | YES |  | 0 |  | 生成代码方式（0zip压缩包 1自定义路径） |
| `gen_path` | `varchar(200)` | YES |  | / |  | 生成路径（不填默认项目路径） |
| `options` | `varchar(1000)` | YES |  | NULL |  | 其它生成选项 |
| `create_by` | `varchar(64)` | YES |  |  |  | 创建者 |
| `create_time` | `datetime` | YES |  | NULL |  | 创建时间 |
| `update_by` | `varchar(64)` | YES |  |  |  | 更新者 |
| `update_time` | `datetime` | YES |  | NULL |  | 更新时间 |
| `remark` | `varchar(500)` | YES |  | NULL |  | 备注 |

索引：

| 索引名 | 唯一 | 字段 |
|---|---:|---|
| `PRIMARY` | 是 | `table_id` |

### `gen_table_column`

用途：若依代码生成字段表，记录每个业务表字段的生成规则。

引擎：`InnoDB`；实际行数：`0`；数据库注释：代码生成业务表字段。

字段：

| 字段 | 类型 | 空 | 键 | 默认值 | 额外 | 注释 |
|---|---|---|---|---|---|---|
| `column_id` | `bigint(20)` | NO | PRI | NULL | auto_increment | 编号 |
| `table_id` | `bigint(20)` | YES |  | NULL |  | 归属表编号 |
| `column_name` | `varchar(200)` | YES |  | NULL |  | 列名称 |
| `column_comment` | `varchar(500)` | YES |  | NULL |  | 列描述 |
| `column_type` | `varchar(100)` | YES |  | NULL |  | 列类型 |
| `python_type` | `varchar(500)` | YES |  | NULL |  | PYTHON类型 |
| `python_field` | `varchar(200)` | YES |  | NULL |  | PYTHON字段名 |
| `is_pk` | `char(1)` | YES |  | NULL |  | 是否主键（1是） |
| `is_increment` | `char(1)` | YES |  | NULL |  | 是否自增（1是） |
| `is_required` | `char(1)` | YES |  | NULL |  | 是否必填（1是） |
| `is_unique` | `char(1)` | YES |  | NULL |  | 是否唯一（1是） |
| `is_insert` | `char(1)` | YES |  | NULL |  | 是否为插入字段（1是） |
| `is_edit` | `char(1)` | YES |  | NULL |  | 是否编辑字段（1是） |
| `is_list` | `char(1)` | YES |  | NULL |  | 是否列表字段（1是） |
| `is_query` | `char(1)` | YES |  | NULL |  | 是否查询字段（1是） |
| `query_type` | `varchar(200)` | YES |  | EQ |  | 查询方式（等于、不等于、大于、小于、范围） |
| `html_type` | `varchar(200)` | YES |  | NULL |  | 显示类型（文本框、文本域、下拉框、复选框、单选框、日期控件） |
| `dict_type` | `varchar(200)` | YES |  |  |  | 字典类型 |
| `sort` | `int(11)` | YES |  | NULL |  | 排序 |
| `create_by` | `varchar(64)` | YES |  |  |  | 创建者 |
| `create_time` | `datetime` | YES |  | NULL |  | 创建时间 |
| `update_by` | `varchar(64)` | YES |  |  |  | 更新者 |
| `update_time` | `datetime` | YES |  | NULL |  | 更新时间 |

索引：

| 索引名 | 唯一 | 字段 |
|---|---:|---|
| `PRIMARY` | 是 | `column_id` |

### `guild_battle`

用途：帮会约战/数据导入主表，承载一次约战或一次导入数据的元信息。

引擎：`InnoDB`；实际行数：`5`；数据库注释：帮会约战表。

字段：

| 字段 | 类型 | 空 | 键 | 默认值 | 额外 | 注释 |
|---|---|---|---|---|---|---|
| `battle_id` | `bigint(20)` | NO | PRI | NULL | auto_increment | 约战ID |
| `battle_name` | `varchar(100)` | YES |  | NULL |  | 约战名称 |
| `battle_date` | `varchar(10)` | YES | MUL | NULL |  | 约战日期(如20260510) |
| `initiator_guild_id` | `bigint(20)` | NO | MUL | NULL |  | 发起方帮会ID |
| `opponent_guild_id` | `bigint(20)` | YES | MUL | NULL |  | 对手帮会ID |
| `opponent_name` | `varchar(50)` | YES |  | NULL |  | 对手帮会名称(未注册时) |
| `battle_time` | `datetime` | YES |  | NULL |  | 约战时间 |
| `battle_type` | `varchar(20)` | YES |  | 友谊赛 |  | 约战类型 |
| `battle_result` | `varchar(10)` | YES |  | NULL |  | 约战结果: 胜利/失败 |
| `status` | `char(1)` | YES |  | 0 |  | 状态(0待确认 1进行中 2已完成 3已取消) |
| `csv_file_url` | `varchar(255)` | YES |  | NULL |  | 上传的CSV文件路径 |
| `create_time` | `datetime` | YES |  | NULL |  | 创建时间 |
| `remark` | `varchar(500)` | YES |  | NULL |  | 备注 |
| `del_flag` | `char(1)` | NO |  | 0 |  | 删除标志 |
| `user_id` | `bigint(20)` | NO |  | 0 |  | 导入人用户ID |
| `my_guild_name` | `varchar(64)` | YES |  |  |  | 自己帮会名称 |

索引：

| 索引名 | 唯一 | 字段 |
|---|---:|---|
| `PRIMARY` | 是 | `battle_id` |
| `idx_battle_date` | 否 | `battle_date` |
| `idx_initiator` | 否 | `initiator_guild_id` |
| `idx_opponent` | 否 | `opponent_guild_id` |

### `guild_battle_invite`

用途：约战报名邀请链接表，用于临时报名入口、邀请码和过期控制。

引擎：`InnoDB`；实际行数：`11`；数据库注释：约战临时邀请链接表。

字段：

| 字段 | 类型 | 空 | 键 | 默认值 | 额外 | 注释 |
|---|---|---|---|---|---|---|
| `invite_id` | `bigint(20)` | NO | PRI | NULL | auto_increment | 邀请ID |
| `invite_code` | `varchar(64)` | NO | UNI | NULL |  | 邀请码 |
| `owner_user_id` | `bigint(20)` | NO | MUL | 0 |  | 帮会大当家用户ID |
| `guild_name` | `varchar(64)` | YES |  |  |  | 帮会名称快照 |
| `battle_name` | `varchar(100)` | NO |  |  |  | 约战名称 |
| `battle_time` | `datetime` | YES |  | NULL |  | 约战时间 |
| `expire_time` | `datetime` | NO | MUL | NULL |  | 链接过期时间 |
| `status` | `char(1)` | NO |  | 0 |  | 状态（0启用 1停用） |
| `create_time` | `datetime` | YES |  | CURRENT_TIMESTAMP |  | 创建时间 |
| `create_by` | `varchar(64)` | YES |  |  |  | 创建者 |
| `remark` | `varchar(500)` | YES |  |  |  | 备注 |
| `del_flag` | `char(1)` | NO |  | 0 |  | 删除标志（0正常 1删除） |

索引：

| 索引名 | 唯一 | 字段 |
|---|---:|---|
| `PRIMARY` | 是 | `invite_id` |
| `idx_guild_battle_invite_expire` | 否 | `expire_time` |
| `idx_guild_battle_invite_owner` | 否 | `owner_user_id` |
| `uk_guild_battle_invite_code` | 是 | `invite_code` |

### `guild_battle_record`

用途：约战玩家明细数据表，是数据分析的核心事实表，保存每名玩家的击败、助攻、伤害、治疗、承伤等统计。

引擎：`InnoDB`；实际行数：`600`；数据库注释：约战玩家明细表。

字段：

| 字段 | 类型 | 空 | 键 | 默认值 | 额外 | 注释 |
|---|---|---|---|---|---|---|
| `record_id` | `bigint(20)` | NO | PRI | NULL | auto_increment | 记录ID |
| `battle_id` | `bigint(20)` | NO | MUL | NULL |  | 所属约战ID |
| `guild_id` | `bigint(20)` | NO | MUL | NULL |  | 所属帮会ID |
| `battle_date` | `varchar(10)` | YES | MUL | NULL |  | 战斗日期(前端解析自文件名) |
| `guild_name` | `varchar(50)` | YES |  | NULL |  | 帮会名称(前端解析自分段头) |
| `player_name` | `varchar(30)` | NO | MUL | NULL |  | 玩家角色名 |
| `player_class` | `varchar(20)` | YES |  | NULL |  | 职业 |
| `kills` | `int(4)` | YES |  | 0 |  | 击败数 |
| `qingquan_kills` | `int(4)` | YES |  | 0 |  | 清泉击败数(前端拆分) |
| `assists` | `int(4)` | YES |  | 0 |  | 助攻数 |
| `resources` | `int(11)` | YES |  | 0 |  | 资源分 |
| `dmg_to_players` | `bigint(20)` | YES |  | 0 |  | 对玩家伤害 |
| `armor_break_players` | `bigint(20)` | YES |  | 0 |  | 对玩家破甲值 |
| `dmg_to_buildings` | `bigint(20)` | YES |  | 0 |  | 对建筑伤害 |
| `armor_break_buildings` | `bigint(20)` | YES |  | 0 |  | 对建筑破甲值 |
| `healing` | `bigint(20)` | YES |  | 0 |  | 治疗值 |
| `dmg_taken` | `bigint(20)` | YES |  | 0 |  | 承受伤害 |
| `deaths` | `int(4)` | YES |  | 0 |  | 重伤次数 |
| `revives` | `int(4)` | YES |  | 0 |  | 复活/清泉次数 |
| `burn_bones` | `int(4)` | YES |  | 0 |  | 焚骨次数 |
| `create_time` | `datetime` | YES |  | NULL |  | 导入时间 |
| `del_flag` | `char(1)` | NO |  | 0 |  | 删除标志 |

索引：

| 索引名 | 唯一 | 字段 |
|---|---:|---|
| `PRIMARY` | 是 | `record_id` |
| `idx_battle_date` | 否 | `battle_date` |
| `idx_battle_id` | 否 | `battle_id` |
| `idx_guild_id` | 否 | `guild_id` |
| `idx_player_name` | 否 | `player_name` |

### `guild_battle_registration`

用途：约战报名表，记录玩家通过邀请链接提交的报名、请假、审核状态。

引擎：`InnoDB`；实际行数：`24`；数据库注释：约战报名表。

字段：

| 字段 | 类型 | 空 | 键 | 默认值 | 额外 | 注释 |
|---|---|---|---|---|---|---|
| `registration_id` | `bigint(20)` | NO | PRI | NULL | auto_increment | 报名ID |
| `invite_id` | `bigint(20)` | YES | MUL | NULL |  | ??ID |
| `invite_code` | `varchar(64)` | YES |  |  |  | ????? |
| `battle_id` | `bigint(20)` | YES | MUL | NULL |  | 约战ID |
| `guild_id` | `bigint(20)` | NO | MUL | NULL |  | 报名帮会ID |
| `owner_user_id` | `bigint(20)` | NO | MUL | 0 |  | ???????ID |
| `applicant_user_id` | `bigint(20)` | NO |  | NULL |  | 申请人用户ID |
| `member_id` | `bigint(20)` | YES | MUL | NULL |  | ??ID |
| `team_id` | `bigint(20)` | YES |  | NULL |  | 参战分团ID |
| `registration_type` | `varchar(20)` | YES |  | signup |  | 申请类型(signup/leave) |
| `player_name` | `varchar(30)` | NO |  |  |  | ????? |
| `player_class` | `varchar(20)` | YES |  |  |  | ??? |
| `secondary_class` | `varchar(20)` | YES |  |  |  | ?? |
| `role_in_guild` | `varchar(20)` | YES |  |  |  | ???? |
| `applicant_name` | `varchar(50)` | YES |  |  |  | ????? |
| `applicant_contact` | `varchar(100)` | YES |  |  |  | ???? |
| `apply_time` | `datetime` | YES |  | NULL |  | 报名时间 |
| `approval_status` | `char(1)` | YES |  | 0 |  | 审核状态(0待审核 1已通过 2已拒绝) |
| `approval_time` | `datetime` | YES |  | NULL |  | 审核时间 |
| `approval_by` | `varchar(64)` | YES |  | NULL |  | 审核人 |
| `approval_comment` | `varchar(500)` | YES |  | NULL |  | 审核备注 |
| `remark` | `varchar(500)` | YES |  |  |  | ?? |
| `del_flag` | `char(1)` | NO |  | 0 |  | ?????0?? 1??? |

索引：

| 索引名 | 唯一 | 字段 |
|---|---:|---|
| `PRIMARY` | 是 | `registration_id` |
| `idx_battle_id` | 否 | `battle_id` |
| `idx_guild_battle_registration_invite` | 否 | `invite_id` |
| `idx_guild_battle_registration_member` | 否 | `member_id` |
| `idx_guild_battle_registration_owner_status` | 否 | `owner_user_id`, `approval_status` |
| `idx_guild_id` | 否 | `guild_id` |

### `guild_class_color`

用途：职业颜色配置表，给职业卡片、表格单元格和分析视图提供视觉颜色。

引擎：`MyISAM`；实际行数：`35`；数据库注释：职业颜色配置表。

字段：

| 字段 | 类型 | 空 | 键 | 默认值 | 额外 | 注释 |
|---|---|---|---|---|---|---|
| `id` | `int(11)` | NO | PRI | NULL | auto_increment | 主键ID |
| `class_name` | `varchar(20)` | NO |  | NULL |  | 职业名称 |
| `bg_color` | `varchar(7)` | NO |  | #FFFFFF |  | 背景颜色 |
| `text_color` | `varchar(7)` | NO |  | #000000 |  | 文字颜色 |
| `user_id` | `bigint(20)` | NO | MUL | 0 |  | 所属用户ID |
| `create_time` | `datetime` | YES |  | CURRENT_TIMESTAMP |  | 创建时间 |

索引：

| 索引名 | 唯一 | 字段 |
|---|---:|---|
| `PRIMARY` | 是 | `id` |
| `uk_user_class` | 是 | `user_id`, `class_name` |

### `guild_info`

用途：帮会信息表，保存帮会基础资料。当前库中暂无数据。

引擎：`InnoDB`；实际行数：`0`；数据库注释：帮会信息表。

字段：

| 字段 | 类型 | 空 | 键 | 默认值 | 额外 | 注释 |
|---|---|---|---|---|---|---|
| `guild_id` | `bigint(20)` | NO | PRI | NULL | auto_increment | 帮会ID |
| `user_id` | `bigint(20)` | NO | UNI | NULL |  | 关联用户ID |
| `guild_name` | `varchar(50)` | YES |  | NULL |  | 帮会名称 |
| `member_count` | `int(4)` | YES |  | 0 |  | 成员数量 |
| `status` | `char(1)` | YES |  | 0 |  | 状态(0正常 1停用) |
| `del_flag` | `char(1)` | YES |  | 0 |  | 删除标志(0存在 2删除) |
| `update_time` | `datetime` | YES |  | NULL |  | 更新时间 |
| `remark` | `varchar(500)` | YES |  | NULL |  | 备注 |

索引：

| 索引名 | 唯一 | 字段 |
|---|---:|---|
| `PRIMARY` | 是 | `guild_id` |
| `uk_user_id` | 是 | `user_id` |

### `guild_join_application`

用途：帮会入会申请表，记录玩家申请加入帮会的资料与审核过程。

引擎：`MyISAM`；实际行数：`7`；数据库注释：帮会入会申请表。

字段：

| 字段 | 类型 | 空 | 键 | 默认值 | 额外 | 注释 |
|---|---|---|---|---|---|---|
| `application_id` | `bigint(20)` | NO | PRI | NULL | auto_increment | 申请ID |
| `applicant_user_id` | `bigint(20)` | NO |  | 0 |  | 申请用户ID |
| `guild_id` | `bigint(20)` | NO |  | 0 |  | 目标帮会ID |
| `guild_name` | `varchar(30)` | NO |  | '' |  | 目标帮会名称快照 |
| `player_name` | `varchar(30)` | NO |  | '' |  | 玩家角色名 |
| `player_class` | `varchar(20)` | YES |  | '' |  | 主职业 |
| `secondary_class` | `varchar(20)` | YES |  | '' |  | 副职 |
| `remark` | `varchar(500)` | YES |  | '' |  | 备注 |
| `review_status` | `char(1)` | NO |  | 0 |  | 审核状态（0待审核 1已通过 2已拒绝） |
| `del_flag` | `char(1)` | NO |  | 0 |  | 删除标志（0有效 1归档） |
| `apply_time` | `datetime` | YES |  | NULL |  | 申请时间 |
| `review_time` | `datetime` | YES |  | NULL |  | 审核时间 |
| `reviewer_user_id` | `bigint(20)` | YES |  | NULL |  | 审核人用户ID |

索引：

| 索引名 | 唯一 | 字段 |
|---|---:|---|
| `PRIMARY` | 是 | `application_id` |

### `guild_member`

用途：帮会成员主数据表，保存玩家名称、职业、副职、状态等，是排表左侧对象抽屉的数据来源。

引擎：`InnoDB`；实际行数：`69`；数据库注释：帮会成员表。

字段：

| 字段 | 类型 | 空 | 键 | 默认值 | 额外 | 注释 |
|---|---|---|---|---|---|---|
| `member_id` | `bigint(20)` | NO | PRI | NULL | auto_increment | 成员ID |
| `guild_id` | `bigint(20)` | NO | MUL | NULL |  | 所属帮会ID |
| `user_id` | `bigint(20)` | NO |  | 0 |  | 所属用户ID |
| `member_user_id` | `bigint(20)` | NO |  | 0 |  | 成员账号用户ID |
| `player_name` | `varchar(30)` | NO | MUL | NULL |  | 玩家角色名 |
| `player_class` | `varchar(20)` | YES |  | NULL |  | 职业 |
| `secondary_class` | `varchar(20)` | YES |  | NULL |  | 副职 |
| `role_in_guild` | `varchar(20)` | YES |  | 成员 |  | 帮会身份 |
| `join_time` | `datetime` | YES |  | NULL |  | 加入时间 |
| `is_active` | `char(1)` | YES |  | 0 |  | 活跃状态(0活跃 1已退出) |
| `source_type` | `varchar(20)` | YES |  | manual |  | 成员来源 |
| `remark` | `varchar(500)` | YES |  | NULL |  | 备注 |
| `team_id` | `bigint(20)` | YES |  | NULL |  | 团队ID |
| `squad_number` | `int(11)` | YES |  | NULL |  | 队编号 |

索引：

| 索引名 | 唯一 | 字段 |
|---|---:|---|
| `PRIMARY` | 是 | `member_id` |
| `idx_guild_id` | 否 | `guild_id` |
| `idx_player_name` | 否 | `player_name` |

### `guild_profession`

用途：职业信息表，维护职业名称、排序、职业颜色等基础配置。

引擎：`MyISAM`；实际行数：`12`；数据库注释：职业信息表。

字段：

| 字段 | 类型 | 空 | 键 | 默认值 | 额外 | 注释 |
|---|---|---|---|---|---|---|
| `profession_id` | `bigint(20)` | NO | PRI | NULL | auto_increment | 职业ID |
| `profession_name` | `varchar(30)` | NO | UNI | NULL |  | 职业名称 |
| `order_num` | `int(11)` | NO |  | 0 |  | 显示顺序 |
| `status` | `char(1)` | NO |  | 0 |  | 状态（0正常 1停用） |
| `create_by` | `varchar(64)` | YES |  |  |  | 创建者 |
| `create_time` | `datetime` | YES |  | NULL |  | 创建时间 |
| `update_by` | `varchar(64)` | YES |  |  |  | 更新者 |
| `update_time` | `datetime` | YES |  | NULL |  | 更新时间 |
| `remark` | `varchar(500)` | YES |  | NULL |  | 备注 |

索引：

| 索引名 | 唯一 | 字段 |
|---|---:|---|
| `PRIMARY` | 是 | `profession_id` |
| `uk_guild_profession_name` | 是 | `profession_name` |

### `guild_review`

用途：审核记录表，用于记录帮会相关审核动作。当前库中暂无数据。

引擎：`InnoDB`；实际行数：`0`；数据库注释：审核记录表。

字段：

| 字段 | 类型 | 空 | 键 | 默认值 | 额外 | 注释 |
|---|---|---|---|---|---|---|
| `review_id` | `bigint(20)` | NO | PRI | NULL | auto_increment | 审核ID |
| `review_type` | `varchar(30)` | NO | MUL | NULL |  | 审核类型 |
| `target_id` | `bigint(20)` | NO | MUL | NULL |  | 审核对象ID |
| `guild_id` | `bigint(20)` | NO | MUL | NULL |  | 所属帮会ID |
| `applicant_id` | `bigint(20)` | NO |  | NULL |  | 申请人用户ID |
| `reviewer_id` | `bigint(20)` | YES |  | NULL |  | 审核人用户ID |
| `review_status` | `char(1)` | YES |  | 0 |  | 审核状态(0待审核 1已通过 2已拒绝) |
| `review_comment` | `varchar(500)` | YES |  | NULL |  | 审核备注 |
| `apply_time` | `datetime` | YES |  | NULL |  | 申请时间 |
| `review_time` | `datetime` | YES |  | NULL |  | 审核时间 |

索引：

| 索引名 | 唯一 | 字段 |
|---|---:|---|
| `PRIMARY` | 是 | `review_id` |
| `idx_guild_id` | 否 | `guild_id` |
| `idx_review_type` | 否 | `review_type` |
| `idx_target_id` | 否 | `target_id` |

### `guild_schedule`

用途：约战排表主表，区分当前排表和历史排表，是团队、小队、自由表格 workbook 的上层记录。

引擎：`InnoDB`；实际行数：`9`；数据库注释：约战排表主表。

字段：

| 字段 | 类型 | 空 | 键 | 默认值 | 额外 | 注释 |
|---|---|---|---|---|---|---|
| `schedule_id` | `bigint(20)` | NO | PRI | NULL | auto_increment | 排表ID |
| `schedule_name` | `varchar(100)` | NO |  |  |  | 排表名称 |
| `user_id` | `bigint(20)` | NO | MUL | 0 |  | 所属用户ID |
| `is_active` | `char(1)` | NO |  | 0 |  | 是否当前排表(0否 1是) |
| `source_schedule_id` | `bigint(20)` | YES |  | NULL |  | 来源排表ID |
| `create_time` | `datetime` | YES |  | CURRENT_TIMESTAMP |  | 创建时间 |
| `update_time` | `datetime` | YES |  | CURRENT_TIMESTAMP | on update CURRENT_TIMESTAMP | 更新时间 |
| `del_flag` | `char(1)` | NO |  | 0 |  | 删除标志(0正常 1删除) |

索引：

| 索引名 | 唯一 | 字段 |
|---|---:|---|
| `PRIMARY` | 是 | `schedule_id` |
| `idx_guild_schedule_user_active` | 否 | `user_id`, `is_active`, `del_flag` |

### `guild_schedule_assignment`

用途：约战排表成员分配表，保存成员被排到哪个团队/小队/槽位。

引擎：`InnoDB`；实际行数：`3`；数据库注释：约战排表成员分配表。

字段：

| 字段 | 类型 | 空 | 键 | 默认值 | 额外 | 注释 |
|---|---|---|---|---|---|---|
| `assignment_id` | `bigint(20)` | NO | PRI | NULL | auto_increment | 分配ID |
| `schedule_id` | `bigint(20)` | NO | MUL | 0 |  | 排表ID |
| `team_id` | `bigint(20)` | NO | MUL | 0 |  | 排表团队ID |
| `squad_id` | `bigint(20)` | NO | MUL | 0 |  | 排表小队ID |
| `member_id` | `bigint(20)` | NO |  | 0 |  | 成员ID |
| `player_name` | `varchar(30)` | NO |  |  |  | 玩家角色名快照 |
| `player_class` | `varchar(20)` | YES |  |  |  | 主职业快照 |
| `secondary_class` | `varchar(20)` | YES |  |  |  | 副职快照 |
| `order_num` | `int(11)` | NO |  | 0 |  | 显示顺序 |
| `remark` | `varchar(500)` | YES |  |  |  | 排表备注 |
| `create_time` | `datetime` | YES |  | CURRENT_TIMESTAMP |  | 创建时间 |
| `update_time` | `datetime` | YES |  | CURRENT_TIMESTAMP | on update CURRENT_TIMESTAMP | 更新时间 |

索引：

| 索引名 | 唯一 | 字段 |
|---|---:|---|
| `PRIMARY` | 是 | `assignment_id` |
| `idx_guild_schedule_assignment_squad` | 否 | `squad_id` |
| `idx_guild_schedule_assignment_team` | 否 | `team_id` |
| `uk_guild_schedule_assignment_member` | 是 | `schedule_id`, `member_id` |

### `guild_schedule_squad`

用途：约战排表小队表，属于某个团队，通常承载 1-6 号位。

引擎：`InnoDB`；实际行数：`4`；数据库注释：约战排表小队表。

字段：

| 字段 | 类型 | 空 | 键 | 默认值 | 额外 | 注释 |
|---|---|---|---|---|---|---|
| `squad_id` | `bigint(20)` | NO | PRI | NULL | auto_increment | 排表小队ID |
| `team_id` | `bigint(20)` | NO | MUL | 0 |  | 排表团队ID |
| `squad_name` | `varchar(50)` | NO |  |  |  | 小队名称 |
| `max_members` | `int(11)` | NO |  | 6 |  | 小队人数上限 |
| `order_num` | `int(11)` | NO |  | 0 |  | 显示顺序 |
| `create_time` | `datetime` | YES |  | CURRENT_TIMESTAMP |  | 创建时间 |
| `update_time` | `datetime` | YES |  | CURRENT_TIMESTAMP | on update CURRENT_TIMESTAMP | 更新时间 |

索引：

| 索引名 | 唯一 | 字段 |
|---|---:|---|
| `PRIMARY` | 是 | `squad_id` |
| `idx_guild_schedule_squad_team` | 否 | `team_id` |

### `guild_schedule_team`

用途：约战排表团队表，属于某个排表，下面包含多个小队。

引擎：`InnoDB`；实际行数：`5`；数据库注释：约战排表团队表。

字段：

| 字段 | 类型 | 空 | 键 | 默认值 | 额外 | 注释 |
|---|---|---|---|---|---|---|
| `team_id` | `bigint(20)` | NO | PRI | NULL | auto_increment | 排表团队ID |
| `schedule_id` | `bigint(20)` | NO | MUL | 0 |  | 排表ID |
| `team_name` | `varchar(50)` | NO |  |  |  | 团队名称 |
| `order_num` | `int(11)` | NO |  | 0 |  | 显示顺序 |
| `create_time` | `datetime` | YES |  | CURRENT_TIMESTAMP |  | 创建时间 |
| `update_time` | `datetime` | YES |  | CURRENT_TIMESTAMP | on update CURRENT_TIMESTAMP | 更新时间 |

索引：

| 索引名 | 唯一 | 字段 |
|---|---:|---|
| `PRIMARY` | 是 | `team_id` |
| `idx_guild_schedule_team_schedule` | 否 | `schedule_id` |

### `guild_schedule_workbook`

用途：约战排表自由 Univer 表格数据表，按 schedule_id 保存整张 workbook_json，用于类 Excel 自由编辑和历史还原。

引擎：`MyISAM`；实际行数：`1`；数据库注释：约战排表自由表格数据表。

字段：

| 字段 | 类型 | 空 | 键 | 默认值 | 额外 | 注释 |
|---|---|---|---|---|---|---|
| `workbook_id` | `bigint(20)` | NO | PRI | NULL | auto_increment | 自由表格ID |
| `schedule_id` | `bigint(20)` | NO | UNI | 0 |  | 排表ID |
| `workbook_json` | `longtext` | NO |  | NULL |  | Univer工作簿JSON |
| `create_time` | `datetime` | YES |  | NULL |  | 创建时间 |
| `update_time` | `datetime` | YES |  | NULL |  | 更新时间 |

索引：

| 索引名 | 唯一 | 字段 |
|---|---:|---|
| `PRIMARY` | 是 | `workbook_id` |
| `schedule_id` | 是 | `schedule_id` |

### `guild_team`

用途：帮会固定团队表，当前库中暂无数据。

引擎：`InnoDB`；实际行数：`0`；数据库注释：帮会团队表。

字段：

| 字段 | 类型 | 空 | 键 | 默认值 | 额外 | 注释 |
|---|---|---|---|---|---|---|
| `id` | `bigint(20)` | NO | PRI | NULL | auto_increment | 主键ID |
| `team_name` | `varchar(50)` | NO | MUL | NULL |  | 团队名称 |
| `team_type` | `varchar(20)` | NO |  | NULL |  | 团队类型：进攻团/防守团 |
| `user_id` | `bigint(20)` | NO |  | 0 |  | 用户ID |
| `create_time` | `datetime` | YES |  | CURRENT_TIMESTAMP |  | 创建时间 |
| `update_time` | `datetime` | YES |  | CURRENT_TIMESTAMP | on update CURRENT_TIMESTAMP | 更新时间 |

索引：

| 索引名 | 唯一 | 字段 |
|---|---:|---|
| `PRIMARY` | 是 | `id` |
| `uk_team_name_type_user` | 是 | `team_name`, `team_type`, `user_id` |

### `sys_config`

用途：系统参数配置表。

引擎：`InnoDB`；实际行数：`9`；数据库注释：参数配置表。

字段：

| 字段 | 类型 | 空 | 键 | 默认值 | 额外 | 注释 |
|---|---|---|---|---|---|---|
| `config_id` | `int(5)` | NO | PRI | NULL | auto_increment | 参数主键 |
| `config_name` | `varchar(100)` | YES |  |  |  | 参数名称 |
| `config_key` | `varchar(100)` | YES |  |  |  | 参数键名 |
| `config_value` | `varchar(500)` | YES |  |  |  | 参数键值 |
| `config_type` | `char(1)` | YES |  | N |  | 系统内置（Y是 N否） |
| `create_by` | `varchar(64)` | YES |  |  |  | 创建者 |
| `create_time` | `datetime` | YES |  | NULL |  | 创建时间 |
| `update_by` | `varchar(64)` | YES |  |  |  | 更新者 |
| `update_time` | `datetime` | YES |  | NULL |  | 更新时间 |
| `remark` | `varchar(500)` | YES |  | NULL |  | 备注 |

索引：

| 索引名 | 唯一 | 字段 |
|---|---:|---|
| `PRIMARY` | 是 | `config_id` |

### `sys_dept`

用途：系统部门表。

引擎：`MyISAM`；实际行数：`0`；数据库注释：部门表。

字段：

| 字段 | 类型 | 空 | 键 | 默认值 | 额外 | 注释 |
|---|---|---|---|---|---|---|
| `dept_id` | `bigint(20)` | NO | PRI | NULL | auto_increment | 部门id |
| `parent_id` | `bigint(20)` | YES |  | 0 |  | 父部门id |
| `ancestors` | `varchar(50)` | YES |  | '' |  | 祖级列表 |
| `dept_name` | `varchar(30)` | YES |  | '' |  | 部门名称 |
| `order_num` | `int(11)` | YES |  | 0 |  | 显示顺序 |
| `leader` | `varchar(20)` | YES |  | NULL |  | 负责人 |
| `phone` | `varchar(11)` | YES |  | NULL |  | 联系电话 |
| `email` | `varchar(50)` | YES |  | NULL |  | 邮箱 |
| `status` | `char(1)` | YES |  | 0 |  | 部门状态（0正常 1停用） |
| `del_flag` | `char(1)` | YES |  | 0 |  | 删除标志（0代表存在 2代表删除） |
| `create_by` | `varchar(64)` | YES |  | '' |  | 创建者 |
| `create_time` | `datetime` | YES |  | NULL |  | 创建时间 |
| `update_by` | `varchar(64)` | YES |  | '' |  | 更新者 |
| `update_time` | `datetime` | YES |  | NULL |  | 更新时间 |

索引：

| 索引名 | 唯一 | 字段 |
|---|---:|---|
| `PRIMARY` | 是 | `dept_id` |

### `sys_dict_data`

用途：系统字典数据表，保存具体字典项。

引擎：`InnoDB`；实际行数：`69`；数据库注释：字典数据表。

字段：

| 字段 | 类型 | 空 | 键 | 默认值 | 额外 | 注释 |
|---|---|---|---|---|---|---|
| `dict_code` | `bigint(20)` | NO | PRI | NULL | auto_increment | 字典编码 |
| `dict_sort` | `int(4)` | YES |  | 0 |  | 字典排序 |
| `dict_label` | `varchar(100)` | YES |  |  |  | 字典标签 |
| `dict_value` | `varchar(100)` | YES |  |  |  | 字典键值 |
| `dict_type` | `varchar(100)` | YES |  |  |  | 字典类型 |
| `css_class` | `varchar(100)` | YES |  | NULL |  | 样式属性（其他样式扩展） |
| `list_class` | `varchar(100)` | YES |  | NULL |  | 表格回显样式 |
| `is_default` | `char(1)` | YES |  | N |  | 是否默认（Y是 N否） |
| `status` | `char(1)` | YES |  | 0 |  | 状态（0正常 1停用） |
| `create_by` | `varchar(64)` | YES |  |  |  | 创建者 |
| `create_time` | `datetime` | YES |  | NULL |  | 创建时间 |
| `update_by` | `varchar(64)` | YES |  |  |  | 更新者 |
| `update_time` | `datetime` | YES |  | NULL |  | 更新时间 |
| `remark` | `varchar(500)` | YES |  | NULL |  | 备注 |

索引：

| 索引名 | 唯一 | 字段 |
|---|---:|---|
| `PRIMARY` | 是 | `dict_code` |

### `sys_dict_type`

用途：系统字典类型表，定义字典分类。

引擎：`InnoDB`；实际行数：`12`；数据库注释：字典类型表。

字段：

| 字段 | 类型 | 空 | 键 | 默认值 | 额外 | 注释 |
|---|---|---|---|---|---|---|
| `dict_id` | `bigint(20)` | NO | PRI | NULL | auto_increment | 字典主键 |
| `dict_name` | `varchar(100)` | YES |  |  |  | 字典名称 |
| `dict_type` | `varchar(100)` | YES | UNI |  |  | 字典类型 |
| `status` | `char(1)` | YES |  | 0 |  | 状态（0正常 1停用） |
| `create_by` | `varchar(64)` | YES |  |  |  | 创建者 |
| `create_time` | `datetime` | YES |  | NULL |  | 创建时间 |
| `update_by` | `varchar(64)` | YES |  |  |  | 更新者 |
| `update_time` | `datetime` | YES |  | NULL |  | 更新时间 |
| `remark` | `varchar(500)` | YES |  | NULL |  | 备注 |

索引：

| 索引名 | 唯一 | 字段 |
|---|---:|---|
| `PRIMARY` | 是 | `dict_id` |
| `dict_type` | 是 | `dict_type` |

### `sys_job`

用途：系统定时任务配置表。

引擎：`InnoDB`；实际行数：`4`；数据库注释：定时任务调度表。

字段：

| 字段 | 类型 | 空 | 键 | 默认值 | 额外 | 注释 |
|---|---|---|---|---|---|---|
| `job_id` | `bigint(20)` | NO | PRI | NULL | auto_increment | 任务ID |
| `job_name` | `varchar(64)` | NO | PRI |  |  | 任务名称 |
| `job_group` | `varchar(64)` | NO | PRI | default |  | 任务组名 |
| `job_executor` | `varchar(64)` | YES |  | default |  | 任务执行器 |
| `invoke_target` | `varchar(500)` | NO |  | NULL |  | 调用目标字符串 |
| `job_args` | `varchar(255)` | YES |  |  |  | 位置参数 |
| `job_kwargs` | `varchar(255)` | YES |  |  |  | 关键字参数 |
| `cron_expression` | `varchar(255)` | YES |  |  |  | cron执行表达式 |
| `misfire_policy` | `varchar(20)` | YES |  | 3 |  | 计划执行错误策略（1立即执行 2执行一次 3放弃执行） |
| `concurrent` | `char(1)` | YES |  | 1 |  | 是否并发执行（0允许 1禁止） |
| `status` | `char(1)` | YES |  | 0 |  | 状态（0正常 1暂停） |
| `create_by` | `varchar(64)` | YES |  |  |  | 创建者 |
| `create_time` | `datetime` | YES |  | NULL |  | 创建时间 |
| `update_by` | `varchar(64)` | YES |  |  |  | 更新者 |
| `update_time` | `datetime` | YES |  | NULL |  | 更新时间 |
| `remark` | `varchar(500)` | YES |  |  |  | 备注信息 |

索引：

| 索引名 | 唯一 | 字段 |
|---|---:|---|
| `PRIMARY` | 是 | `job_id`, `job_name`, `job_group` |

### `sys_job_log`

用途：系统定时任务日志表。

引擎：`InnoDB`；实际行数：`0`；数据库注释：定时任务调度日志表。

字段：

| 字段 | 类型 | 空 | 键 | 默认值 | 额外 | 注释 |
|---|---|---|---|---|---|---|
| `job_log_id` | `bigint(20)` | NO | PRI | NULL | auto_increment | 任务日志ID |
| `job_name` | `varchar(64)` | NO |  | NULL |  | 任务名称 |
| `job_group` | `varchar(64)` | NO |  | NULL |  | 任务组名 |
| `job_executor` | `varchar(64)` | NO |  | NULL |  | 任务执行器 |
| `invoke_target` | `varchar(500)` | NO |  | NULL |  | 调用目标字符串 |
| `job_args` | `varchar(255)` | YES |  |  |  | 位置参数 |
| `job_kwargs` | `varchar(255)` | YES |  |  |  | 关键字参数 |
| `job_trigger` | `varchar(255)` | YES |  |  |  | 任务触发器 |
| `job_message` | `varchar(500)` | YES |  | NULL |  | 日志信息 |
| `status` | `char(1)` | YES |  | 0 |  | 执行状态（0正常 1失败） |
| `exception_info` | `varchar(2000)` | YES |  |  |  | 异常信息 |
| `create_time` | `datetime` | YES |  | NULL |  | 创建时间 |

索引：

| 索引名 | 唯一 | 字段 |
|---|---:|---|
| `PRIMARY` | 是 | `job_log_id` |

### `sys_logininfor`

用途：系统登录日志表。

引擎：`InnoDB`；实际行数：`0`；数据库注释：系统访问记录。

字段：

| 字段 | 类型 | 空 | 键 | 默认值 | 额外 | 注释 |
|---|---|---|---|---|---|---|
| `info_id` | `bigint(20)` | NO | PRI | NULL | auto_increment | 访问ID |
| `user_name` | `varchar(50)` | YES |  |  |  | 用户账号 |
| `ipaddr` | `varchar(128)` | YES |  |  |  | 登录IP地址 |
| `login_location` | `varchar(255)` | YES |  |  |  | 登录地点 |
| `browser` | `varchar(50)` | YES |  |  |  | 浏览器类型 |
| `os` | `varchar(50)` | YES |  |  |  | 操作系统 |
| `status` | `char(1)` | YES | MUL | 0 |  | 登录状态（0成功 1失败） |
| `msg` | `varchar(255)` | YES |  |  |  | 提示消息 |
| `login_time` | `datetime` | YES | MUL | NULL |  | 访问时间 |

索引：

| 索引名 | 唯一 | 字段 |
|---|---:|---|
| `PRIMARY` | 是 | `info_id` |
| `idx_sys_logininfor_lt` | 否 | `login_time` |
| `idx_sys_logininfor_s` | 否 | `status` |

### `sys_menu`

用途：系统菜单和权限标识表。

引擎：`InnoDB`；实际行数：`115`；数据库注释：菜单权限表。

字段：

| 字段 | 类型 | 空 | 键 | 默认值 | 额外 | 注释 |
|---|---|---|---|---|---|---|
| `menu_id` | `bigint(20)` | NO | PRI | NULL | auto_increment | 菜单ID |
| `menu_name` | `varchar(50)` | NO |  | NULL |  | 菜单名称 |
| `parent_id` | `bigint(20)` | YES |  | 0 |  | 父菜单ID |
| `order_num` | `int(4)` | YES |  | 0 |  | 显示顺序 |
| `path` | `varchar(200)` | YES |  |  |  | 路由地址 |
| `component` | `varchar(255)` | YES |  | NULL |  | 组件路径 |
| `query` | `varchar(255)` | YES |  | NULL |  | 路由参数 |
| `route_name` | `varchar(50)` | YES |  |  |  | 路由名称 |
| `is_frame` | `int(1)` | YES |  | 1 |  | 是否为外链（0是 1否） |
| `is_cache` | `int(1)` | YES |  | 0 |  | 是否缓存（0缓存 1不缓存） |
| `menu_type` | `char(1)` | YES |  |  |  | 菜单类型（M目录 C菜单 F按钮） |
| `visible` | `char(1)` | YES |  | 0 |  | 菜单状态（0显示 1隐藏） |
| `status` | `char(1)` | YES |  | 0 |  | 菜单状态（0正常 1停用） |
| `perms` | `varchar(100)` | YES |  | NULL |  | 权限标识 |
| `icon` | `varchar(100)` | YES |  | # |  | 菜单图标 |
| `create_by` | `varchar(64)` | YES |  |  |  | 创建者 |
| `create_time` | `datetime` | YES |  | NULL |  | 创建时间 |
| `update_by` | `varchar(64)` | YES |  |  |  | 更新者 |
| `update_time` | `datetime` | YES |  | NULL |  | 更新时间 |
| `remark` | `varchar(500)` | YES |  |  |  | 备注 |

索引：

| 索引名 | 唯一 | 字段 |
|---|---:|---|
| `PRIMARY` | 是 | `menu_id` |

### `sys_notice`

用途：系统通知公告表。

引擎：`InnoDB`；实际行数：`1`；数据库注释：通知公告表。

字段：

| 字段 | 类型 | 空 | 键 | 默认值 | 额外 | 注释 |
|---|---|---|---|---|---|---|
| `notice_id` | `int(4)` | NO | PRI | NULL | auto_increment | 公告ID |
| `notice_title` | `varchar(50)` | NO |  | NULL |  | 公告标题 |
| `notice_type` | `char(1)` | NO |  | NULL |  | 公告类型（1通知 2公告） |
| `notice_content` | `longblob` | YES |  | NULL |  | 公告内容 |
| `status` | `char(1)` | YES |  | 0 |  | 公告状态（0正常 1关闭） |
| `create_by` | `varchar(64)` | YES |  |  |  | 创建者 |
| `create_time` | `datetime` | YES |  | NULL |  | 创建时间 |
| `update_by` | `varchar(64)` | YES |  |  |  | 更新者 |
| `update_time` | `datetime` | YES |  | NULL |  | 更新时间 |
| `remark` | `varchar(255)` | YES |  | NULL |  | 备注 |

索引：

| 索引名 | 唯一 | 字段 |
|---|---:|---|
| `PRIMARY` | 是 | `notice_id` |

### `sys_oper_log`

用途：系统操作日志表。

引擎：`InnoDB`；实际行数：`0`；数据库注释：操作日志记录。

字段：

| 字段 | 类型 | 空 | 键 | 默认值 | 额外 | 注释 |
|---|---|---|---|---|---|---|
| `oper_id` | `bigint(20)` | NO | PRI | NULL | auto_increment | 日志主键 |
| `title` | `varchar(50)` | YES |  |  |  | 模块标题 |
| `business_type` | `int(2)` | YES | MUL | 0 |  | 业务类型（0其它 1新增 2修改 3删除） |
| `method` | `varchar(100)` | YES |  |  |  | 方法名称 |
| `request_method` | `varchar(10)` | YES |  |  |  | 请求方式 |
| `operator_type` | `int(1)` | YES |  | 0 |  | 操作类别（0其它 1后台用户 2手机端用户） |
| `oper_name` | `varchar(50)` | YES |  |  |  | 操作人员 |
| `dept_name` | `varchar(50)` | YES |  |  |  | 部门名称 |
| `oper_url` | `varchar(255)` | YES |  |  |  | 请求URL |
| `oper_ip` | `varchar(128)` | YES |  |  |  | 主机地址 |
| `oper_location` | `varchar(255)` | YES |  |  |  | 操作地点 |
| `oper_param` | `varchar(2000)` | YES |  |  |  | 请求参数 |
| `json_result` | `varchar(2000)` | YES |  |  |  | 返回参数 |
| `status` | `int(1)` | YES | MUL | 0 |  | 操作状态（0正常 1异常） |
| `error_msg` | `varchar(2000)` | YES |  |  |  | 错误消息 |
| `oper_time` | `datetime` | YES | MUL | NULL |  | 操作时间 |
| `cost_time` | `bigint(20)` | YES |  | 0 |  | 消耗时间 |

索引：

| 索引名 | 唯一 | 字段 |
|---|---:|---|
| `PRIMARY` | 是 | `oper_id` |
| `idx_sys_oper_log_bt` | 否 | `business_type` |
| `idx_sys_oper_log_ot` | 否 | `oper_time` |
| `idx_sys_oper_log_s` | 否 | `status` |

### `sys_role`

用途：系统角色表。

引擎：`InnoDB`；实际行数：`3`；数据库注释：角色信息表。

字段：

| 字段 | 类型 | 空 | 键 | 默认值 | 额外 | 注释 |
|---|---|---|---|---|---|---|
| `role_id` | `bigint(20)` | NO | PRI | NULL | auto_increment | 角色ID |
| `role_name` | `varchar(30)` | NO |  | NULL |  | 角色名称 |
| `role_key` | `varchar(100)` | NO |  | NULL |  | 角色权限字符串 |
| `role_sort` | `int(4)` | NO |  | NULL |  | 显示顺序 |
| `data_scope` | `char(1)` | YES |  | 1 |  | 数据范围（1：全部数据权限 2：自定数据权限 3：本部门数据权限 4：本部门及以下数据权限） |
| `menu_check_strictly` | `tinyint(1)` | YES |  | 1 |  | 菜单树选择项是否关联显示 |
| `dept_check_strictly` | `tinyint(1)` | YES |  | 1 |  | 部门树选择项是否关联显示 |
| `status` | `char(1)` | NO |  | NULL |  | 角色状态（0正常 1停用） |
| `del_flag` | `char(1)` | YES |  | 0 |  | 删除标志（0代表存在 2代表删除） |
| `create_by` | `varchar(64)` | YES |  |  |  | 创建者 |
| `create_time` | `datetime` | YES |  | NULL |  | 创建时间 |
| `update_by` | `varchar(64)` | YES |  |  |  | 更新者 |
| `update_time` | `datetime` | YES |  | NULL |  | 更新时间 |
| `remark` | `varchar(500)` | YES |  | NULL |  | 备注 |

索引：

| 索引名 | 唯一 | 字段 |
|---|---:|---|
| `PRIMARY` | 是 | `role_id` |

### `sys_role_dept`

用途：角色和部门关联表。

引擎：`MyISAM`；实际行数：`0`；数据库注释：角色和部门关联表。

字段：

| 字段 | 类型 | 空 | 键 | 默认值 | 额外 | 注释 |
|---|---|---|---|---|---|---|
| `role_id` | `bigint(20)` | NO | PRI | NULL |  | 角色ID |
| `dept_id` | `bigint(20)` | NO | PRI | NULL |  | 部门ID |

索引：

| 索引名 | 唯一 | 字段 |
|---|---:|---|
| `PRIMARY` | 是 | `role_id`, `dept_id` |

### `sys_role_menu`

用途：角色和菜单关联表。

引擎：`InnoDB`；实际行数：`84`；数据库注释：角色和菜单关联表。

字段：

| 字段 | 类型 | 空 | 键 | 默认值 | 额外 | 注释 |
|---|---|---|---|---|---|---|
| `role_id` | `bigint(20)` | NO | PRI | NULL |  | 角色ID |
| `menu_id` | `bigint(20)` | NO | PRI | NULL |  | 菜单ID |

索引：

| 索引名 | 唯一 | 字段 |
|---|---:|---|
| `PRIMARY` | 是 | `role_id`, `menu_id` |

### `sys_user`

用途：系统用户表。

引擎：`InnoDB`；实际行数：`20`；数据库注释：用户信息表。

字段：

| 字段 | 类型 | 空 | 键 | 默认值 | 额外 | 注释 |
|---|---|---|---|---|---|---|
| `user_id` | `bigint(20)` | NO | PRI | NULL | auto_increment | 用户ID |
| `dept_id` | `bigint(20)` | YES |  | NULL |  | 部门ID |
| `user_name` | `varchar(30)` | NO |  | NULL |  | 用户账号 |
| `nick_name` | `varchar(30)` | NO |  | NULL |  | 用户昵称 |
| `user_type` | `varchar(2)` | YES |  | 00 |  | 用户类型（00系统用户） |
| `email` | `varchar(50)` | YES |  |  |  | 用户邮箱 |
| `phonenumber` | `varchar(11)` | YES |  |  |  | 手机号码 |
| `sex` | `char(1)` | YES |  | 0 |  | 用户性别（0男 1女 2未知） |
| `avatar` | `varchar(100)` | YES |  |  |  | 头像地址 |
| `password` | `varchar(100)` | YES |  |  |  | 密码 |
| `status` | `char(1)` | YES |  | 0 |  | 帐号状态（0正常 1停用） |
| `del_flag` | `char(1)` | YES |  | 0 |  | 删除标志（0代表存在 2代表删除） |
| `login_ip` | `varchar(128)` | YES |  |  |  | 最后登录IP |
| `login_date` | `datetime` | YES |  | NULL |  | 最后登录时间 |
| `pwd_update_date` | `datetime` | YES |  | NULL |  | 密码最后更新时间 |
| `create_by` | `varchar(64)` | YES |  |  |  | 创建者 |
| `create_time` | `datetime` | YES |  | NULL |  | 创建时间 |
| `update_by` | `varchar(64)` | YES |  |  |  | 更新者 |
| `update_time` | `datetime` | YES |  | NULL |  | 更新时间 |
| `remark` | `varchar(500)` | YES |  | NULL |  | 备注 |

索引：

| 索引名 | 唯一 | 字段 |
|---|---:|---|
| `PRIMARY` | 是 | `user_id` |

### `sys_user_role`

用途：用户和角色关联表。

引擎：`InnoDB`；实际行数：`3`；数据库注释：用户和角色关联表。

字段：

| 字段 | 类型 | 空 | 键 | 默认值 | 额外 | 注释 |
|---|---|---|---|---|---|---|
| `user_id` | `bigint(20)` | NO | PRI | NULL |  | 用户ID |
| `role_id` | `bigint(20)` | NO | PRI | NULL |  | 角色ID |

索引：

| 索引名 | 唯一 | 字段 |
|---|---:|---|
| `PRIMARY` | 是 | `user_id`, `role_id` |

