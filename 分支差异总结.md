# GitHub 远程分支差异总结

生成时间：2026-06-10

## 1. 远程分支列表

当前 GitHub 远程仓库 `origin` 上有 3 个分支：

| 分支 | 提交 | 提交说明 |
| --- | --- | --- |
| `origin/main` | `ffb38ac` | `feat: update guild workflows and class color experience` |
| `origin/codex/animated-login-theme` | `0e72521` | `feat: disable captcha for auth` |
| `origin/codex/register-user-cleanup` | `1b3096c` | `feat: add self registration cleanup rule` |

默认远程分支：

```text
origin/HEAD -> origin/main
```

## 2. 分支关系

这 3 个分支不是互相分叉的三条开发线，而是一条线性演进历史：

```text
origin/codex/register-user-cleanup
  -> origin/codex/animated-login-theme
    -> origin/main
```

也就是说：

- `origin/codex/register-user-cleanup` 的内容已经包含在 `origin/codex/animated-login-theme` 中。
- `origin/codex/animated-login-theme` 的内容已经包含在 `origin/main` 中。
- `origin/main` 是当前最新、最完整的分支。
- 两个 `codex/*` 分支没有比 `main` 更新的独有提交。

## 3. `origin/codex/register-user-cleanup` 的具体差异

相对父提交 `18c02c6`，该分支主要新增“自注册用户清理规则”。

### 3.1 后端接口

新增接口：

| 方法 | 路径 | 用途 | 权限 |
| --- | --- | --- | --- |
| `GET` | `/system/user/register-cleanup-rule` | 获取注册用户清理规则状态 | `system:user:list` |
| `PUT` | `/system/user/register-cleanup-rule` | 开启或关闭注册用户清理规则 | `system:user:edit` |

涉及文件：

- `ruoyi-fastapi-backend/module_admin/controller/user_controller.py`
- `ruoyi-fastapi-backend/module_admin/service/user_service.py`
- `ruoyi-fastapi-backend/module_admin/entity/vo/user_vo.py`

### 3.2 配置和定时任务

新增配置键：

```text
sys.account.cleanupInactiveRegisteredUsers
```

新增定时任务：

| 项 | 值 |
| --- | --- |
| 任务名称 | `注册用户24小时未登录自动清理` |
| 调用目标 | `module_task.user_cleanup.cleanup_inactive_registered_users` |
| Cron | `0 0 * * * ?` |
| 默认状态 | 关闭 |

涉及文件：

- `ruoyi-fastapi-backend/module_admin/dao/job_dao.py`
- `ruoyi-fastapi-backend/module_task/user_cleanup.py`
- `ruoyi-fastapi-backend/sql/20260607_register_cleanup_rule.sql`

### 3.3 清理规则

清理任务会软删除满足以下条件的用户：

- `del_flag == '0'`
- `user_id != 1`
- `create_by == 'register'`
- `login_date IS NULL`
- `create_time <= 当前时间 - 24小时`

命中后更新：

```text
del_flag = '2'
update_by = 'system'
update_time = 当前时间
```

涉及文件：

- `ruoyi-fastapi-backend/module_admin/dao/user_dao.py`

### 3.4 注册逻辑变化

自助注册用户新增默认信息：

- `createBy = register`
- `updateBy = register`
- 默认分配 `user` 角色
- 如果没有 `user` 角色，则回退到 `common` 角色

涉及文件：

- `ruoyi-fastapi-backend/module_admin/service/login_service.py`

### 3.5 前端变化

用户管理页面新增开关：

```text
清理未登录注册用户
```

该开关会调用：

- `getRegisterCleanupRule()`
- `updateRegisterCleanupRule(data)`

涉及文件：

- `ruoyi-fastapi-frontend/src/api/system/user.js`
- `ruoyi-fastapi-frontend/src/views/system/user/index.vue`

## 4. `origin/codex/animated-login-theme` 的具体差异

该分支在 `origin/codex/register-user-cleanup` 基础上多 2 个提交：

```text
dcd90fe feat: redesign auth pages with animated theme
0e72521 feat: disable captcha for auth
```

### 4.1 登录和注册页面重做

登录页和注册页从原来的简单表单，改为带动画角色的双栏视觉页面。

主要变化：

- 新增左侧视觉区域。
- 新增动画角色。
- 登录页支持显示/隐藏密码。
- 登录成功、失败会影响动画状态。
- 注册页支持显示/隐藏密码和确认密码。
- 注册页新增密码强度条。
- 移除原验证码输入区域。

新增组件：

- `ruoyi-fastapi-frontend/src/components/AuthCharacters/AnimatedCharacters.vue`
- `ruoyi-fastapi-frontend/src/components/AuthCharacters/EyeBall.vue`
- `ruoyi-fastapi-frontend/src/components/AuthCharacters/Pupil.vue`

主要修改文件：

- `ruoyi-fastapi-frontend/src/views/login.vue`
- `ruoyi-fastapi-frontend/src/views/register.vue`

### 4.2 验证码关闭

登录 API 从：

```js
login(username, password, code, uuid)
```

改为：

```js
login(username, password)
```

前端删除：

- `code`
- `uuid`
- 验证码图片加载
- 验证码表单校验规则

涉及文件：

- `ruoyi-fastapi-frontend/src/api/login.js`
- `ruoyi-fastapi-frontend/src/store/modules/user.js`
- `ruoyi-fastapi-frontend/src/views/login.vue`
- `ruoyi-fastapi-frontend/src/views/register.vue`

### 4.3 新增认证页公开配置接口

新增接口：

```text
GET /authConfig
```

返回：

```text
registerEnabled
```

用途：

- 登录页判断是否展示“立即注册”入口。
- 避免登录页继续依赖验证码接口获取注册开关。

涉及文件：

- `ruoyi-fastapi-backend/module_admin/controller/login_controller.py`
- `ruoyi-fastapi-backend/module_admin/entity/vo/login_vo.py`

### 4.4 验证码接口兼容处理

当验证码关闭时，`/captchaImage` 直接返回：

```json
{
  "captchaEnabled": false,
  "registerEnabled": true,
  "img": "",
  "uuid": ""
}
```

涉及文件：

- `ruoyi-fastapi-backend/module_admin/controller/captcha_controller.py`

### 4.5 SQL 默认配置变化

默认关闭验证码：

```text
sys.account.captchaEnabled = false
```

涉及文件：

- `ruoyi-fastapi-backend/sql/ruoyi-fastapi.sql`
- `ruoyi-fastapi-backend/sql/ruoyi-fastapi-pg.sql`
- `ruoyi-fastapi-backend/sql/20260607_register_cleanup_rule.sql`

## 5. `origin/main` 的具体差异

`origin/main` 在 `origin/codex/animated-login-theme` 基础上多 4 个提交：

```text
9a8519d feat: add liquid glass scss skill
e081765 feat: refresh admin shell and database tools
7c51068 style: tighten sidebar submenu spacing
ffb38ac feat: update guild workflows and class color experience
```

### 5.1 新增 Codex 技能文件

新增：

- `.codex/skills/liquid-glass-scss/SKILL.md`

用途：

- 记录 liquid glass 风格 SCSS 的实现约定。

### 5.2 新增数据库管理功能

新增只读数据库管理模块，仅超级管理员可访问。

后端新增接口：

| 方法 | 路径 | 用途 | 权限 |
| --- | --- | --- | --- |
| `GET` | `/system/database/overview` | 获取数据库总览 | `system:database:list` |
| `GET` | `/system/database/users` | 获取全部用户数据总览 | `system:database:list` |
| `GET` | `/system/database/tables/{table_name}/columns` | 获取表字段 | `system:database:list` |
| `GET` | `/system/database/tables/{table_name}/rows` | 获取表分页数据 | `system:database:query` |

后端特性：

- 仅超级管理员可访问。
- 支持 MySQL 和 PostgreSQL。
- 表名做合法性校验。
- 表数据分页查询最大 `pageSize = 100`。
- 数据只读，不提供写入、删除、更新能力。

涉及文件：

- `ruoyi-fastapi-backend/module_admin/controller/database_controller.py`
- `ruoyi-fastapi-backend/module_admin/entity/vo/database_vo.py`
- `ruoyi-fastapi-backend/module_admin/service/database_service.py`
- `ruoyi-fastapi-backend/sql/20260609_system_database_admin.sql`

前端新增：

- `ruoyi-fastapi-frontend/src/api/system/database.js`
- `ruoyi-fastapi-frontend/src/views/system/database/index.vue`

页面能力：

- 数据库总览
- 表列表
- 表名搜索
- 表数据分页浏览
- 表字段结构浏览
- 全部用户数据总览

### 5.3 后台外壳和样式刷新

主要修改：

- 侧边栏样式
- 导航栏样式
- 标签页样式
- 面包屑样式
- 主内容区域布局
- Element Plus 全局样式
- 新增 `liquid-glass.scss`

涉及文件：

- `ruoyi-fastapi-frontend/src/assets/styles/element-ui.scss`
- `ruoyi-fastapi-frontend/src/assets/styles/index.scss`
- `ruoyi-fastapi-frontend/src/assets/styles/liquid-glass.scss`
- `ruoyi-fastapi-frontend/src/assets/styles/ruoyi.scss`
- `ruoyi-fastapi-frontend/src/assets/styles/sidebar.scss`
- `ruoyi-fastapi-frontend/src/assets/styles/variables.module.scss`
- `ruoyi-fastapi-frontend/src/components/Breadcrumb/index.vue`
- `ruoyi-fastapi-frontend/src/layout/components/AppMain.vue`
- `ruoyi-fastapi-frontend/src/layout/components/Navbar.vue`
- `ruoyi-fastapi-frontend/src/layout/components/Sidebar/Logo.vue`
- `ruoyi-fastapi-frontend/src/layout/components/TagsView/index.vue`
- `ruoyi-fastapi-frontend/src/layout/index.vue`

### 5.4 帮会信息功能完善

新增后端接口：

| 方法 | 路径 | 用途 | 权限 |
| --- | --- | --- | --- |
| `GET` | `/guild/member/guild-info` | 获取帮会信息 | `guild:member:list` |
| `PUT` | `/guild/member/guild-name` | 修改帮会名称 | `guild:member:edit` |

后端返回的帮会信息包括：

- `guild_id`
- `guild_name`
- `member_count`
- `class_count`
- `class_stats`

帮会名称实际保存到当前用户的 `nick_name` 字段。

涉及文件：

- `ruoyi-fastapi-backend/module_guild/controller/member_controller.py`
- `ruoyi-fastapi-backend/module_guild/entity/vo/member_vo.py`
- `ruoyi-fastapi-backend/module_guild/service/member_service.py`

前端帮会信息页从“功能开发中”改为完整页面：

- 帮会名称编辑
- 成员数量统计
- 职业数量统计
- 人数最多职业
- 职业人数分布条

涉及文件：

- `ruoyi-fastapi-frontend/src/api/guild/member.js`
- `ruoyi-fastapi-frontend/src/views/guild/info/index.vue`

### 5.5 职业颜色工具抽取

新增：

- `ruoyi-fastapi-frontend/src/utils/guildClassColor.js`

提供能力：

- 加载职业颜色配置
- 规范化接口返回字段
- 生成职业标签样式
- 生成职业分布条样式
- 暴露 `useGuildClassColors()` 供页面复用

影响页面包括：

- 帮会信息
- 职业颜色设置
- 成员、审核、约战、排表等相关页面

### 5.6 公开约战邀请页调整

主要变化：

- 默认 tab 从“申请加入帮会”改为“约战报名”。
- 成员搜索从表格改成下拉选择。
- 搜索时展示“搜索中”、“没有匹配成员”等状态。
- 选中成员后自动填充本次报名基础信息。
- 报名提交字段减少，不再提交申请人和联系方式。

报名提交现在主要包含：

- `member_id`
- `player_class`
- `secondary_class`
- `remark`

涉及文件：

- `ruoyi-fastapi-frontend/src/views/public/battleInvite/index.vue`

### 5.7 成员管理权限补齐

新增 SQL：

- `ruoyi-fastapi-backend/sql/20260610_common_guild_member_permissions.sql`

新增或补齐权限：

- `guild:member:add`
- `guild:member:edit`
- `guild:member:remove`
- `guild:member:import`

绑定角色：

- `admin`
- `common`

用途：

- 确保普通帮会管理者可以管理自己帮会内的成员。

## 6. 分支差异统计

### 6.1 `register-user-cleanup` 到 `animated-login-theme`

范围：

```text
origin/codex/register-user-cleanup..origin/codex/animated-login-theme
```

统计：

- 20 个文件变化
- 约 1991 行新增
- 约 365 行删除

核心内容：

- 登录/注册页动画主题
- 关闭验证码
- 新增 `/authConfig`

### 6.2 `animated-login-theme` 到 `main`

范围：

```text
origin/codex/animated-login-theme..origin/main
```

统计：

- 40 个文件变化
- 约 4027 行新增
- 约 475 行删除

核心内容：

- 数据库管理功能
- 后台外壳样式刷新
- 帮会信息和职业颜色体验增强
- 公开约战报名体验调整
- 成员管理权限补齐

### 6.3 `register-user-cleanup` 到 `main`

范围：

```text
origin/codex/register-user-cleanup..origin/main
```

统计：

- 56 个文件变化
- 约 5962 行新增
- 约 784 行删除

核心内容：

- 包含登录注册改造
- 包含验证码关闭
- 包含数据库管理
- 包含帮会流程更新
- 包含后台外壳样式更新

## 7. 建议

### 7.1 后续开发基准

建议以后以 `origin/main` 作为开发基准。

原因：

- `main` 已包含另外两个分支全部内容。
- `main` 是远程默认分支。
- 两个 `codex/*` 分支没有独有的新提交。

### 7.2 分支清理建议

如果确认不需要保留阶段性开发分支，可以考虑删除：

```text
origin/codex/register-user-cleanup
origin/codex/animated-login-theme
```

删除前建议确认：

- 是否还有未合并的 Pull Request。
- 是否需要保留这些分支作为历史标记。
- 是否有部署、自动化脚本或协作者仍引用这些分支。

### 7.3 风险点

需要重点关注：

- 验证码被默认关闭后，公网环境可能增加撞库和恶意注册风险。
- 数据库管理虽然是只读，但能查看大量敏感数据，必须保证只有超级管理员有权限。
- 自注册清理任务依赖 `create_by = register` 和 `login_date IS NULL`，历史注册用户如果字段不规范，可能不会被清理。
- 帮会名称保存在用户 `nick_name` 字段中，如果后续需要独立帮会实体，可能需要迁移。

