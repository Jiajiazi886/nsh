# 小程序、网页与微信机器人：后端改造草案

日期：2026-09-13。状态：暂拟设计，待业务确认，未实现新接口。

本次沿用你指定的 `xiaochengxu` 分支，在独立 Git 副本中提交文档：

- 原项目（只读）：`E:/nsh/nshls/RuoYi-Vue3-FastAPI-master`
- 工作副本：`E:/nsh/nshls/RuoYi-Vue3-FastAPI-miniapp-backend`
- 小程序参考（只读）：`E:/nsh/nshls/xiaocghengxu-dewmo`
- 基线提交：`86cedd14da360f82724b410451f2158954cd9a5a`

副本有独立的 `.git`，不是共享原项目 Git 元数据的 worktree。只复制原分支已提交内容；原项目未跟踪文件、运行环境和本地数据库不会被提交到副本。参考文档只读使用。

## 阅读顺序

1. [后端改造设计](docs/plans/2026-09-13-unified-backend-design.md)：为什么改、复用什么、改哪些模块、权限和数据关系。
2. [接口契约草案](docs/plans/2026-09-13-unified-backend-api-draft.md)：网页/小程序页面对应接口、请求示例、指标映射与错误约定。
3. [实施路线与验收清单](docs/plans/2026-09-13-unified-backend-roadmap.md)：开发顺序、安全隔离、迁移策略、测试和待确认问题。

## 一句话方案

复用现有若依账号、JWT/Redis 会话、权限、成员和比赛记录；新增独立活动阵容与快照模型，网页拖拽和小程序点击操作同一份数据，机器人通过可替换通道调用相同服务。

## 本次没有做什么

- 没有修改原项目或小程序的任何文件，也没有切换原项目分支。
- 没有编写、启用或声称已经完成新后端接口。
- 没有启动后端、连接数据库、执行 SQL/Alembic 或导入生产数据。
- 没有运行会创建表或初始化数据的后端测试。
- 没有推送远程、合并分支、发送微信消息或操作开发者工具。

文档中的路径、表名、接口和默认规则分为“现有实现”“拟新增”“待确认”；不能把拟新增接口当成已经可调用，也不能把另一个 Demo 的演示规则直接当成正式业务。
