FROM nginx:latest
WORKDIR /usr/share/nginx/html

# 用于资源受限服务器：先在构建机执行 npm run build:docker，
# 再仅封装 dist 与生产 Nginx 配置。
COPY dist .
COPY bin/nginx.dockermy.conf /etc/nginx/conf.d/default.conf

EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
