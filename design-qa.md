# 约战详情页设计 QA

- 检查日期：2026-09-15
- 视觉目标：把旧约战详情页压缩为“活动摘要、主要操作、当前阵容、战报数据”四个清晰区域，并将 CSV 上传直接放入当前约战。
- 源视觉真值：`C:\Users\ASUS\.codex\attachments\bffd3b01-eb6b-45c6-b42f-0b730455a664\image-1.png`
- 实现截图：`E:\nsh\nshls\RuoYi-Vue3-FastAPI-miniapp-backend\.artifacts\battle-detail\implementation-desktop.png`
- CSV 弹窗截图：`E:\nsh\nshls\RuoYi-Vue3-FastAPI-miniapp-backend\.artifacts\battle-detail\implementation-import-dialog.png`
- 窄屏截图：`E:\nsh\nshls\RuoYi-Vue3-FastAPI-miniapp-backend\.artifacts\battle-detail\implementation-narrow.png`
- 同图对比：`E:\nsh\nshls\RuoYi-Vue3-FastAPI-miniapp-backend\.artifacts\battle-detail\comparison.png`

## 对比条件

- 桌面视口：1648 × 1040 CSS px，`deviceScaleFactor: 1`。
- 窄屏视口：920 × 1040 CSS px，`deviceScaleFactor: 1`。
- 源图像素：1649 × 1075；实现桌面截图：1648 × 1050；CSV 弹窗截图：1648 × 1040；窄屏截图：920 × 1050。
- 密度归一：源图和实现均按 1 倍像素直接比较；源图仅比实现多 1 px 宽，不构成可见密度差异。
- 页面状态：已登录管理员、公开活动、已有 v4 阵容、18 席、9 个空缺、暂无战报。
- 数据状态：所有浏览器请求均由隔离夹具响应，没有读取或写入真实后端业务数据。

## 全视图比较证据

旧界面把活动说明、报名说明、阵容和战报操作拆成大块卡片，首屏留白多，且使用“关联已有 CSV 战报”要求手工输入战报 ID。新界面把名称、时间、人数、空缺、版本、战报状态和职业缺口收进同一摘要卡片；主要操作集中在摘要下方；两团在桌面并排、小队和六席紧凑排列；无战报状态缩为一行提示。首屏能够同时看到活动摘要、完整阵容和战报状态。

## 重点区域比较证据

- CSV 导入：点击顶部“上传 CSV”直接打开“上传 CSV 战报”弹窗；选中合法 CSV 后可见玩家数、帮会数、帮会实际人数、比赛日期、结果和双方名称字段。界面明确说明文件直接归属当前约战，不再输入战报 ID。
- 空缺位置：统一使用浅黄色底、黄色边框和“空缺”文字；有职业要求的位置同时显示职业色标。
- 窄屏：920 px 下团队改为单列，小队仍保持六席横向紧凑显示，页面无水平溢出。

## 五项保真检查

- 字体与排版：沿用项目字体和 Element Plus 控件字体；标题、元数据、统计值、队名、玩家名层级清楚；长玩家名使用省略号，不挤压相邻位置。
- 间距与布局节奏：活动摘要、阵容、战报三段之间间距一致；移除旧详情大面积空白；操作按钮只保留一组，战报空状态压缩为一行。
- 颜色与视觉令牌：保持简单浅色；主要操作为蓝色，危险操作为淡红色；空缺为浅黄；素问、铁衣、沧澜、潮光、九灵等职业均显示独立颜色。
- 图像质量与素材：本页面没有业务插图、Logo 或需要复刻的图片素材；未使用占位图替代源视觉内容。
- 文案与内容：已移除“关联已有 CSV 战报”和手工战报 ID；使用“上传 CSV”“CSV 与当前约战直接关联”“只展示最后一次成功保存的版本”等明确文案。

## 对比历史

### 第一次实现检查

- [P2] CSV 上传按钮在摘要和战报区重复出现，增加视觉噪声。
- [P2] 暂无战报时使用较大的空状态组件，页面底部留白偏多，不符合“简化详情”的目标。

处理：保留顶部唯一“上传 CSV”入口；删除战报区重复按钮；将大空状态改为紧凑的一行说明。

### 修正后检查

- 新桌面截图显示顶部只有一组主要操作，战报区高度明显缩小。
- 1648 px 和 920 px 两个视口都没有水平溢出。
- CSV 上传、合法文件解析、摘要展示、弹窗取消均通过隔离浏览器交互测试。
- 浏览器控制台和页面脚本错误数为 0。
- 当前没有仍需处理的 P0、P1 或 P2 问题。

## 交互检查

- 打开约战详情。
- 点击“上传 CSV”。
- 选择固定格式 CSV。
- 检查解析摘要和表单。
- 取消弹窗。
- 切换至 920 px 窄屏并检查水平溢出。
- 检查旧“关联已有 CSV 战报”和“输入战报 ID”均不存在。

## 后续微调

- [P3] 全局后台侧边栏仍占用较多桌面宽度，但属于系统公共布局，不是本次约战详情页改造范围。

final result: passed
