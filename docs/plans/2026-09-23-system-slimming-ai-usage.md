# 实施计划

1. 先补 Usage 提取、记录生命周期、权限与隐私回归测试，再实现记录实体、服务和查询接口。
2. 将测试对话和四类图片识别统一接入记录器，验证上游成功、失败、缺少 Usage 与业务解析失败。
3. 将大模型管理页改为“模型连接 / Token请求记录”双标签并加入筛选、汇总和万单位展示。
4. 新增 AI 额度策略表和环境变量配置，替换 `sys_config` 依赖。
5. 移除字典与旧审计日志代码，业务页面改为固定选项，登录会话信息改为请求内直接生成。
6. 加入不可逆版本化迁移，更新 MySQL/PostgreSQL 初始化脚本和菜单基线。
7. 运行后端回归、前端构建和本地数据库验收；通过后提交推送，再执行服务器同版本迁移和冒烟测试。
