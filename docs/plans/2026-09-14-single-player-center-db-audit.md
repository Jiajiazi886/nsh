# 统一个人中心与开发库审计

仅修改独立副本 `RuoYi-Vue3-FastAPI-miniapp-backend`，仅访问 `nsh_activity_dev_20260914`，原毕业项目和原库保持不动。

用户已要求废弃旧个人中心，继续使用已确认的玩家资料设计。

1. 补测试：旧 `/user/profile` 及旧修改密码链接跳转统一个人中心；头像入口直达同页，不再引用旧资料视图。
2. 保留 `/personal/profile-edit` 为唯一资料表单和规范入口。旧 URL 仅兼容重定向，旧账号资料布局撤除；密码修改保留为独立折叠／弹窗，不与游戏资料混存。
3. 只读审计当前开发库的表、行数、索引和运行时代码引用，不能将“空表”直接判断为无用表。
4. 根据实际查询补必要的非破坏性索引，迁移必须有目标库全名保护且可重复运行；不自动删表、删数据或合并账号与成员记录。
5. 隔离回归、前端构建和真实开发库无头浏览器验证；交付数据库审计清单，待明确淘汰模块及备份方案后再单独删表。

保持职业颜色、个人资料隐私、账号统一、原有活动与快照不变；不自动提交 Git，不操作用户桌面。

## 完成记录

旧页面四个文件已撤除，密码组件已移到独立弹窗。旧 URL、旧密码深链接、头像入口通过真实无头浏览器验证。开发库已重复验证非破坏性索引迁移，第二次无新增；396 项后端隔离测试、75 项 JavaScript 回归及前端构建通过。新版前后端已重启并联调，旧 9100 服务保持原进程。

没有删表或清空历史数据；疑似遗留表与模块退役建议交付于 `docs/开发库表用途审计与个人中心统一.md`。
