# 约战排表盒子化实施清单

用户已于 2026-09-13 确认采用对应设计。修改原项目指定前端范围，保留其他已有功能。

1. 先新增 Node 测试并运行失败：六位置构造、真实 ID/order_num、旧超员/重复位置保留、临时位置、元数据保留、恢复重映射、职业分类、菜单权限继承和幂等插入。
2. 实现纯函数模型和动态菜单辅助函数，再运行测试通过。
3. 新增盒子组件，接现有团队/小队/分配接口。折叠仅由独立按钮触发；删除需要确认；实际成员与临时成员分别保存；失败不先改变本地状态。
4. 新增团队管理占位页，动态分团路由中插入占位入口，不改数据库菜单。
5. 仅替换约战排表右侧及其数据连接，左侧 template 与现有样式原样保留。当前主视图不再导入/挂载 Univer，旧历史表格放入只读兼容折叠区域。
6. 历史应用后，对临时位置以结构锚点重映射并持久化；无法映射的人员明确显示待安排，不清空名单。
7. Node 测试、Vue SFC 编译、隔离无头交互检查（使用模拟接口，不修改真实排表）；检查编译/构建结果及原项目 diff。
8. 只切换本任务已启动的前端进程到原项目前端目录，继续使用已启动的副本后端与授权的 ruoyi 库。不打开/操作用户桌面。
9. 审计完整目标全部满足后标记完成；不合并/推送，不修改后端，不创建测试成员或真实测试排表。

全流程开发的 references 文件在本机缺失，按已完整读取的主技能执行先测试、后实现、再验证。用户未选择子代理执行，本轮由当前代理实施，不派发其他代理。

## 实施与验证结果

2026-09-13 完成原项目 `xiaochengxu` 分支的指定前端改造，代码保留在工作区，未提交、合并或推送原项目。

- 原项目 `schedule/index.vue` 的左侧成员 section 与修改前逐字比较一致（统一换行后），左侧和共享样式一致。
- 当前约战排表不再导入或挂载 `ScheduleUniverSheet`，不再显示当前表格选区、合并及导入/导出工具。历史中的旧 Excel 只读兼容入口保留。
- 新增盒子组件、六位置纯函数模型、临时名单/位置元数据兼容、历史新 ID 重映射、前端菜单辅助函数及“团队管理：暂未开发”占位页。
- Node 回归：新增 15 项通过，连同原有 4 个测试文件合计 19 项通过，无失败。
- Vue SFC 编译：约战排表、新盒子、团队管理占位组件全部通过。
- 隔离无头 Chrome 交互：团队/小队创建、六位置、拖入、交换、点击选人、仅按钮折叠、临时替补、元数据保存失败不污染旧状态、取消创建后保存历史、历史预览/应用重映射、刷新持久化、清除/删除、占位菜单、旧表格未编队人员不虚报已排，全部通过，无未处理页面异常。
- 交互测试所有 API 均拦截为模拟响应，共 80 次模拟请求，真实 API 写入为 0，未创建真实测试成员或修改真实排表。
- 生产构建 `npm run build:prod` 退出码 0，7062 模块，54.38 秒。现有第三方 `use client` 忽略提示和大包提示不阻止构建。
- 原项目前端从原目录提供服务：`http://127.0.0.1/guild/group/schedule`；新增占位 `http://127.0.0.1/guild/group/team-management`。两地址、既有后端文档与代理 authConfig 均 HTTP 200；业务交互验证以无头测试为依据，不仅以 SPA 200 为依据。
- 仅改原项目 8 个前端文件，未改后端、SQL、环境配置或预先存在的用户文件。不操作用户桌面或可见浏览器。

测试脚本位于原项目前端 `src/views/guild/schedule/utils/`。无头截图位于原项目被忽略的 `logs/startup/schedule-boxes-headless.png`，截图展示模拟测试数据，不代表真实帮会阵容。
