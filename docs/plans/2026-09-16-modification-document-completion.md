# 《修改文档》实施与验收记录

日期：2026-09-16

范围只包含独立副本 `RuoYi-Vue3-FastAPI-miniapp-backend` 和开发库 `nsh_activity_dev_20260914`。原毕业项目、原 `ruoyi` 数据库和旧服务没有写入。

## 实施结果

| 修改文档要求 | 结果 | 实现证据 |
| --- | --- | --- |
| 约战与个人中心交互使用 shadcn-vue | 已完成 | 列表、详情、创建活动、阵容编辑、CSV 导入、战报查看、个人中心、帮会信息和分析选择器已改用本地 shadcn-vue 组件；源码契约测试禁止这些页面回退到 Element 表单控件。 |
| 帮会信息只保留成员数，职业列表只显示人数 | 已完成 | 已去掉职业总数、最多职业和百分比。 |
| 删除分团管理与审核管理，约战排表独立 | 已完成 | 旧路由、页面和自由表格编辑器已退役；排表使用稳定独立路由。 |
| 活动自动请假链接，公开手机页确认请假，请假后自动移出阵容 | 已完成 | 管理员可复制链接并查看请假人与时间；公开页支持姓名检索、可选备注和二次确认。 |
| 分析使用历史保存阵容 + 固定 CSV，不显示图表 | 已完成 | 默认最新快照，可选历史快照；分析结果使用分析盒子。 |
| 新活动默认最新排表，可选历史 | 已完成 | 创建活动与阵容编辑均可选择组织历史快照。 |
| 俱乐部仅超级管理员创建 | 已完成 | 后端执行身份校验，不仅依赖前端隐藏。 |
| 位置备注可组合预设并支持自定义 | 已完成 | 支持指挥、保镖、增益绝、奶绝、抗拆、防守、约定、落霞、击杀的组合和自定义文本，随快照保存。 |
| 找约战只在个人管理 | 已完成 | 帮会菜单不再提供入口。 |
| 个人“我的约战”只显示本人参加的帮会与俱乐部活动 | 已完成 | 网页和两份小程序统一传递 `participatingOnly=true`，真实开发库联调已验证未参加俱乐部活动不出现。 |
| 紧凑活动卡片、取消战报状态、职业缺口直接显示数量 | 已完成 | 使用紧凑卡片和小型详情按钮，缺口格式为“职业 + 数量”。 |
| 候选区单开职业折叠，显示剩余/全部 | 已完成 | 所有职业默认收起，同时最多展开一个，有折叠动画。 |
| 历史战报改为历史约战，到期活动可上传 CSV | 已完成 | 网页和两份小程序名称已统一；小程序不再请求独立战报列表。 |
| 数据库整理 | 已完成安全范围 | 仅在白名单开发库删除已确认空且无依赖的 `guild_info` 和 `guild_review`，保留恢复 SQL；本次补齐请假链接与请假记录两张空表。 |

## 验收证据

- 后端隔离集成测试：255 项通过。
- 网页文档相关测试：43 项通过。
- 主小程序测试：31 项通过，JavaScript/JSON 工程检查通过。
- 联赛分析 Demo 测试：15 项通过，1 项需用户原始 CSV 文件的测试因当前环境没有该路径而跳过。
- 前端生产构建：7644 个模块完成构建。仅保留项目原有的 `%VITE_APP_TITLE%` 环境变量警告。
- 真实开发库 + 隔离无头浏览器：真实登录、本人参与活动、详情、阵容编辑、后端快照保存、职业树默认收起全部通过，没有模拟 API，没有 500 响应。
- 数据库只读审计：当前目标库为 `nsh_activity_dev_20260914`，共 58 张表，`guild_info`/`guild_review` 不存在，玩家资料关联查询命中复合索引。

## 原文截断项

`因此帮会管理里面的职业信息这个功能` 和 `文件内的` 两句在原文中未写完。本次没有猜测并执行可能破坏现有功能的删除或数据处理；已完成其他可明确验收的条目。
