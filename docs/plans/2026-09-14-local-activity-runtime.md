# 新版网页与小程序本地实际联调

用户已要求连接后端并查看可用新版，按已批准的独立副本和开发库方案执行。

1. 只读确认现有 MySQL、Redis、旧服务占用；不关闭旧服务。
2. 先测试开发库、端口与日志脱敏的保护条件。
3. 从 `ruoyi` 一次性只读复制现有账号和组织数据至全新 `nsh_activity_dev_20260914`，在该库安装新约战表；不迁移旧库。
4. 新版服务使用 9101、网页 5173、独立 Redis DB15。开发启动不执行复制的定时任务。
5. 网页代理与两个小程序根地址统一指向 9101；保持简单现有界面。
6. 实际检查能力、验证码、登录页、列表接口和数据库持久化；不打印密码或 token，不自动登录真实用户。
7. 留下启动、停止和打开新版的明确入口。新版网页和小程序实时共享开发库；旧版账号数据仅为此次初始复制，不宣称两库后续自动同步。
