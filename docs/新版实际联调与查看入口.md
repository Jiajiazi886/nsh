# 新版实际联调与查看入口

2026-09-14：已实际连接 MySQL、Redis，启动独立新版网页和后端。

## 现在怎么看

网页：`http://127.0.0.1:5173/personal/battle-information/mine`。

个人中心：`http://127.0.0.1:5173/personal/profile-edit`。旧 `/user/profile` 已统一重定向，头像菜单与个人管理进入同一个游戏玩家资料表单。找约战可通过左侧“约战信息 → 找约战”进入。

后端接口文档：`http://127.0.0.1:9101/docs`。

在微信开发者工具打开独立副本中的 `xiaochengxu`；如果继续用原联赛盒子 demo，则打开独立副本的 `xiaochengxu/hhhhhhtml/jiusi-data-dashboard-frontend/demo`。两个目录中的开发地址都已经是 `http://127.0.0.1:9101`。不要打开原毕业项目中的旧目录。

小程序重新编译后，使用下面的演示账号登录，“活动”中的帮会／俱乐部、公开活动、历史活动直接读取真实新版接口和数据库，不回退到假接口。

## 演示账号（仅本地开发库）

| 账号 | 用途 |
| --- | --- |
| `nsh_demo_manager` | 帮会管理员：编排团队、小队、玩家、指定职业、临时替补，保存及公开发布 |
| `nsh_demo_assistant` | 本帮会助手：组织内管理权限 |
| `nsh_demo_member` | 本帮会普通成员：只读已保存阵容、查看俱乐部活动 |
| `nsh_demo_guest` | 外部玩家：只能访问公开活动，绑定素问角色可按要求报名 |
| `nsh_demo_unjoined` | 未入帮账号：仍可填写并保存自己的玩家资料 |

五个演示账号使用本机环境变量 `NSH_DEMO_PASSWORD` 指定的密码，仓库不保存真实值。这些不是原库账号。服务只绑定本机回环地址；不要把演示密码和演示账号部署到公网。

也可使用此次从原库复制来的现有网页账号密码登录新版。原库的 24 个账号、组织资料和业务基础表初始复制到了独立开发库；后续原库账号修改不会自动同步到开发库。**新版网页与小程序实时共享同一个开发库和账号体系；旧版网页保持原样。**

## 已准备的效果

- 60 位可按职业树查找的示例帮会玩家，演示管理员、助手、普通成员均按真实账号 ID 绑定。
- 组织内部约战：3 个团、10 个小队、60 个位置，49 个预排位置（其中一个为仅当前活动有效的临时替补）。
- 公开抢凳子约战：可查空位和指定职业，外部玩家符合要求才能报名。
- 俱乐部练习：独立俱乐部活动，在“我的约战”右侧显示。
- 历史活动：已结束并冻结阵容，无 CSV 时明确显示“暂无战报数据”。
- 原有未关联战报继续保留，不按活动标题或日期猜测绑定。

网页操作：帮会管理／个人管理 → 约战信息 → 我的约战 → 查看详情 → 配置排表。候选职业树默认收起，团在屏宽达到 1200px 时两列、较窄时一列；团内小队竖排，每队六席横排，编辑位置高度 85px；可以创建／删除团和小队、设置位置职业、仅拖拽安排／移动／交换玩家，点击位置不再弹出选人窗口；双向职业校验失败保留原阵容，空缺席位使用浅黄色。每 10 秒仅保存本地 IndexedDB，点击“保存”才写入后端并产生新快照。保存后小程序与普通成员读取同一版本。

小程序活动详情当前提供阵容查看、公开报名／请假和管理员发布／结束操作；完整阵容编辑入口仍在网页。旧离线联赛盒子继续作为离线分析，不冒充共享活动 CSV 战报。

## 当前运行环境

| 项目 | 新版配置 |
| --- | --- |
| 代码 | `E:/nsh/nshls/RuoYi-Vue3-FastAPI-miniapp-backend`，分支 `xiaochengxu` |
| MySQL | `nsh_activity_dev_20260914`，当前 58 张表；已包含请假链接和请假记录表，两个经审计确认无引用的旧空表已定点清理 |
| Redis | 原 Redis 实例的独立 DB15；没有清空任何库 |
| 后端 | 127.0.0.1:9101，新能力已启用，允许网页和小程序分别保有登录会话 |
| 网页 | 127.0.0.1:5173，`/dev-api` 代理至 9101 |

原毕业项目、原 `ruoyi` 数据库及旧 9100／80 服务未被修改或关闭。开发启动不会执行复制来的定时任务，日志启用脱敏。

手机真机不能用电脑的 `127.0.0.1`。当前先在开发者工具看效果；如需真机，后续需单独配置可访问的测试后端和请求域名。本次未上传或发布微信小程序。

## 下次启动

在独立副本目录执行：

```powershell
powershell -ExecutionPolicy Bypass -File .\启动新版联调.ps1
```

新版脚本使用 UTF-8 BOM，已实际通过 Windows PowerShell 5 启动验证，避免中文提示导致脚本解析失败。

不要运行旧 `启动项目.ps1`；新版脚本显式使用受保护的开发启动入口，不会将后端指向 `ruoyi`，不会自动新建、覆盖或重置现有开发数据。

源码入口为 `ruoyi-fastapi-backend/tools/local_activity_dev.py`。`prepare` 只允许首次创建全新的独立开发库，库已存在时拒绝覆盖；本次已经执行过，不要重复初始化。`seed_activity_dev.py` 仅用于开发库演示数据与真实 API 验证，不修改真实账号密码。

## 实际验证

- MySQL 和 Redis 连接成功，`/api/v1/capabilities` 返回约战与快照能力已启用。
- 实际网页版旧登录契约与小程序 JSON 登录契约对应同一个演示账号 ID。
- 两个小程序真实服务封装经过 wx.request 运行时适配，实际读取同一份 60 席保存快照；这不是微信开发者工具或真机验收。
- 所属普通成员读取相同快照但不能管理，组织外演示账号访问私有活动被拒绝，公开与历史活动可读取。
- 真实公开报名验证通过：外部素问玩家报名指定神相位置被拒绝；报名素问位置成功，占位与空位数更新；请假成功，空位恢复。
- 保存组织时出现的真实异步 ORM 对象过期问题已复现、修复并新增 3 项回归测试，不改变全局会话设置。
- 真实网页无头浏览器验证通过：账号密码登录 → 帮会／俱乐部活动 → 详情 → 阵容编辑 → 点击保存，后端成功产生快照；候选职业树默认收起，过程中无 500 响应。不使用用户浏览器，也不模拟 API 数据。
- 新详情编辑按钮与旧嵌套菜单路由不一致的问题已修复，加入稳定的隐藏编辑入口，不授予组织外账号管理权限。
- 396 项后端隔离测试、75 项 JavaScript 回归测试通过，小程序 JS／JSON 工程检查通过。
- 原离线盒子分析另有 15 项测试通过；1 项依赖外部原始 CSV 的测试跳过，未将其计为通过。
- 前端生产构建通过；两个小程序真实表单通过实际后端联调，但这不能替代微信开发者工具编译及真机验收。
- 新个人中心不依赖帮会成员记录：名字、字符串 UID、手填微信号、橙武开关、主副职业和备注统一保存；网页与两个小程序读取同一账号资料。UID 保留前导零，不是登录账号 ID。
- 资料保存与本人有效绑定成员同步在同一事务；没有成员记录时不创建成员，不修改旧快照、已保存阵容或战报。下一次阵容保存重新校验最新职业。
- 微信号只通过本人资料接口及校验实际帮会管理关系的管理接口读取；普通成员、组织外用户、俱乐部管理者和公开活动接口不能获得微信号，请求日志启用脱敏。
- 职业样式读取 `/api/v1/profession-styles`：使用本人已有配置，保留自定义色（包括白色），退出／切换账号清除缓存；失败只回退默认显示颜色，不生成假活动。
- 新增紧凑界面截图：`.artifacts/activity-dev/紧凑拖拽排表.png`、`.artifacts/activity-dev/统一个人中心.png`、`.artifacts/activity-dev/未入帮个人中心.png`。详细契约、安全与命令见《排表与个人资料互通验收》。

日志位于独立副本 `logs/activity-dev`。如实际使用有错误，将界面截图、操作步骤或错误消息反馈给我；我不操作你的桌面和开发者工具。

数据库已增加本人账号／实际帮会范围查询索引；2026-09-16 复审后仅删除 0 行且无任何代码或数据库依赖的 `guild_info`、`guild_review`，当前 56 张表。迁移只允许目标开发库、非空会停止，并保留恢复 SQL；完整清单见 `docs/开发库表用途审计与个人中心统一.md`。
